% !TeX root = ../AnalyticStacks.tex

\section{\ufs Complements on solid modules (Scholze)}

\url{https://www.youtube.com/watch?v=KKzt6C9ggWA&list=PLx5f8IelFRgGmu6gmL-Kf_Rl_6Mm7juZO}
\renewcommand{\yt}[2]{\href{https://www.youtube.com/watch?v=KKzt6C9ggWA&list=PLx5f8IelFRgGmu6gmL-Kf_Rl_6Mm7juZO&t=#1}{#2}}
\vspace{1em}

Today I want to give some complements on solid modules, before Dustin takes over on Wednesday again.

\subsection{Recap of previous lecture}

Let's briefly recall the definition. So $P$ has always denoted the projective object which is, as a free $\Z$-module, $\Z[\N \cup \infty]$, the nullsequences. 
\be
P = \Z[\N \cup \infty] / \Z[\infty]
\ee

Then we considered this endomorphism $f$ of $P$ which is the identity minus the shift, which takes the generator $n$ to $[n+1] - [n]$.
\begin{align*}
f = \text{id-shift$\cl$ } P \to P
\end{align*}
\begin{align*}
[n] \mapsto [n+1] - [n]
\end{align*}

Then the new definition of a solid module was, though entirely analogous to the old definition, but on $P$-modules.

\bd[Solid]
$M \in \Cond\Ab^\light$ is \textit{solid} if the map induced by $f$ is an isomorphism.
\be
f^\ast \cl \IntHom(P,M) \iso \IntHom(P,M)
\ee

% 2:07
Intuitively speaking, $f$ is a map from $P$ to $M$, the space of nullsequences in $M$. So you take a nullsequence in $M$ and map it to the sequence of differences.
\begin{align*}
(m_1, m_2, \ldots) \mapsto (m_0 - m_1, m_1 - m_2, \ldots)
\end{align*}
\ed

Intuitively speaking, that this is an isomorphism means that conversely, if you have any nullsequence in $M$, then you can sum it. Because this $m_0$ must be the sum of all--

% 2:40
\quest This looks like some definition of derived complete modules, like $p$-adically derived complete. I suppose there is some mathematical relation to derived completeness.

\ans I think on the face of it, it is not related. But okay, there will be some derived completeness today.

The theorem we proved last time, was that this category of solid $P$-modules $\in \Cond\Ab^\light$ is itself abelian and is stable under all filtered colimits (let me just say limits and colimits), under tensor products, and also under internal $\Hom$, making the left adjoint to this inclusion an "absolute solidification", making this left adjoint a symmetric monoidal functor. 

\begin{theorem} \label{theorem-06-solid}
$\Solid \subs \Cond\Ab^\light$ is abelian, stable under limits and colimits
acquires a tensor product $\tnsr^\solid$, making left adjoint called "solidification".
\begin{align*}
\Cond\Ab^\light \to \Solid
\end{align*}
\begin{align*}
M \mapsto M^\solid
\end{align*}
symmetric monoidal
$A$ is generated by single compact projective object $\prod_\N \Z$

On this generator, we can compute the solid tensor product, because it just becomes identity ... \note{did not hear this}
\be
\prod_\N \Z \tnsr^\solid \prod_\N \Z \simeq \prod_{\N \times \N} \Z
\ee

\end{theorem}

This projective generator is $P$ itself, and on this generator the solidification functor is the identity.

% 5:02
Maybe the other thing I should say is that one can compute on the solidification of all these free modules on a profinite set $S$, they are just the limit of the free modules on the finite sets $S_n$, if $S$ is a light profinite set.

\be
\Z[S]^\solid \iso \lim_n \Z[S_n] \quad & \text{if } S = \lim_n S_n \in \Pro_\N (\Fin)
\ee

% 5:28
I think this summarizes everything from last time.

Let me first discuss the derived enhancement of the situation.

\subsection{Derived categories}

% 6:10
Let's assume that instead of just one condensed abelian group, you have a derived category of light condensed abelian groups $D(\Cond\Ab^\light)$. Then the definition still makes sense, and we take that as a definition.

% 6:30
We say that a complex of light condensed abelian groups is \textit{solid} if the internal $\RHom(P,M)$ is an isomorphism. 

But again, because the internal $\Hom$ from $P$ is exact, this is equivalent to asking that for all $i \in \Z$, the internal $\Hom$ from $P$ to $H^i(A)$ is an isomorphism. 

% 7;23
So this is equivalent to asking that all the homologies of $A$ are also solid.

So we have:
\bd
$A \in D(\Cond\Ab^\light)$ is \textit{solid} if the internal $\RHom(P,M)$ is an isomorphism.
\be
f^\ast \cl \IntRHom(P,A) \iso \IntRHom(P,A)
\ee
\be
\iff \forall i \in \Z \cl \IntHom(P, H^i(A)) \iso \IntHom(P, H^i(A))
\ee
\be
\iff \text{all } H^i(A) \text{ are solid}.
\ee
\ed

% 7:32
Again, this is also true in our previous discussion of $P$ being $\Z$-modules. In fact, it is a general property for what we call an "analytic ring structure", that you can check completeness at the level of homology groups. Technically, the third equivalence is a little subtle to prove, but the first equivalence is an immediate consequence of the internal projectivity of $P$.

% 8:05
Now some corollaries of this will be stated. 

The class of solid complexes is itself a triangulated subcategory, or if you would work in the $\infty$-categorical setting, it would be a stable $\infty$-subcategory. It is stable under all direct sums and products, so all limits and colimits.

\bc
The class of solid $A$ is a triangulated subcategory and is stable under infinite direct sums and products.
\ec

\begin{unfinished}{9:00}

Again, one can show it is also stable under $\IntRHom$, again from the same proof as last time. 
% 9:23
Then if you just repeat this discussion of tensor products now at the level of derived categories, you actually see that...
% 9:38
It is also true that ... because in the end, it just turns out that if you take the derived category of solid guys 
... functorial so you get a map

% 10:05
\bp
\be
D(\Solid) \mono D(\Cond\Ab^\light) \label{06:eq-derived}
\ee
is fully faithful, and the essential image is exactly the solid $A$'s.

has left adjoint
\be
A \mapsto A^{L\solid} 
\ee

+ unique symmetric monoidal $\tnsr^{L\solid}$ on $D(\Solid)$
making $A \mapsto A^{L\solid}$ symmetric monoidal.
(= derived functor of $\tnsr^\solid$)
\ep

% 10:25
This is something that is not completely automatic. This uses that these projective generators $\prod_\N \Z$ \ref{theorem-06-solid} here somehow have expected vanishing also in light condensed abelian groups. Or equivalently, that the derived solidification actually here $M \mapsto M^\solid$ is 
Or, rather, that is the solidification here.

% 10:55
\ques Is it the case that if you have any complex, possibly unbounded, whose terms are direct sums of those generators, these generators. Then taking solidification term by term is a good derived functor? 

\ans Yes, that is right. Solidification preserves, on the level of---maybe forget about the thing.

Then you have this whole subcategory in the derived category of solid objects. 
% 11:35
Again, by adjunction, it will have a left adjoint, some derived solidification functor, which again will commute with all colimits back into $D(\Cond(\Ab))$. 
%
Then this, on the generators, we determined what it does last time, that it becomes this thing, even in the derived sense.

% 12:05
Also, on the level of derived categories, we have a full inclusion. So it is as nice as it could be. 
Again, by some adjoint functor theorem, this \ref{06:eq-derived} has a left adjoint. Let me write this as derived solidification $L\solid$:
\be
A \mapsto A^{L\solid} 
\ee

% 12:38
\ques In the adjoint functor theorem, there is usually some set-theoretical condition, because you want...

% 12:45
\ans I am thinking of everything as $\infty$-categories, and then everything here is a presentable $\infty$-category. 
Then Lurie has this general adjoint functor theorem there. 
%
I think that for triangulated categories, there are theorems of Neeman \note{not sure of spelling of this name} and others which would also justify this, I believe. 
% 13:02
They often assume actual compact generation, which is not quite true in this case, so I am not sure. But Lurie's version is definitely sufficient.

% 13:19
We would not need the abstract theorem, because we can just prove the existence by hand in this case. 
% 13:23
Because on a class of generators, we have identified that the left adjoint exists.
 
% 13:30
\ques But the abstract theorem is both for left adjoints and right adjoints, assuming that there are enough limits and colimits?

% 13:38
\ans So the notion of presentable $\infty$-category is one which has all colimits and which is generated by the set of objects. 
% 13:47
In this situation, every functor that preserves all colimits has a right adjoint, and every functor that preserves all limits and all sufficiently filtered colimits, ... \note{todo} has a left adjoint.
All sufficiently filtered colimits, so it is accessible ones.

% todo add ref
% 14:15
There is a unique symmetric monoidal derived tensor product $\tnsr^{L\solid}$ on $D(\Solid)$, making 
\be
A \mapsto A^{L\solid} 
\ee
symmetric monoidal.

% 14:47
Something even better is true. This \ref{} is literally the left derived functor of the thing you have between abelian categories. 
% 14:55
Similarly, the derived tensor product is also obtained by deriving the thing you already have on the abelian level.

% 15:16
\ques But here, how do you know that the solid category is presentable in yours, before you proved the existence? 

\ans Yes, I think there are general theorems that if you just ask locality for such a map, there are some automatic presentability theorems.

% 16:25
So once we pass to the derived setting, I can state things from last time. 
That the derived solidification of $\Z[S]$ is still the limit $\underline\Z[S]$. So it is---under solidification, there are no higher homotopies. 
\be
\Z[S]^{L\solid} \iso \lim_n \Z[S_n] = \Z[S]^{\solid}
\ee

% 16:46
Also, the derived solidification of $P$ is this product of $\Z$.
\be
P^{L\solid} \iso \prod_\N \Z
\ee

The derived tensor product of $\Z[S]$ with $\Z_p$ is the condensed ring $\underline{\Z_p}[S]$. \note{todo}
% 16:59
\be
\prod_\N \Z \tnsr^{L\solid} \prod_\N \Z \iso \prod_{\N \times \N} \Z
\ee

% 17:10
You might think that nothing derived ever appears in practice, but this is not so. 

% 17:33
Here is a proposition. Again, let's say that $X$ is a CW complex.

% 
Recall that in this situation, we previously had this thing that the $\Ext$-groups of $\Z$ are the singular cohomology groups.
\be
\Ext^i (\Z[X], \Z) = H^i_\sing (X, \Z)
\ee

% 17:47
But now we can also interpret the homology of $X$. More precisely, it turns out that the singular homology of $X$ is exactly the homology of the derived solidification.

% 18:18
More precisely, there is some complex computing singular homology. This is isomorphic to the derived solidification. In particular, the derived solid solidification is discrete. It comes from the complex of the discrete abelian groups.

\bp
Let $X$ be a CW complex.
\be
H_i^\sing(X,\Z) = H_i(\Z[\underline{X}]^{L\solid})
\ee
Better:
\be
C^\sing(X,\Z) \iso_{\quasi} \Z[\underline{X}]^{L\solid} \label{eq:06-quasi}
\ee
\ep

% 19:04
So again, this is some kind of the version of $\Hom$ isomorphism, that if you take discrete abelian groups on $X$ and this models the homology of $X$.

% todo missing text

\bex
If you take $\Z[]^{L\solid} \iso \Z$

% 19:45
But if you take the circle and solidify, that becomes a copy of $\Z$ of degree 0 and $\Z$ of degree 1.

\begin{align*}
\Z[]^{L\solid} \iso \Z 
\end{align*}

\eex

% 19:57
In general, if you take the disjoint union of circles of all dimensions, you get something which is concentrated in degree zero, but the derived solidification goes arbitrarily far to the left.

% 21:06
%\ques About \ref{eq:06-quasi} I may miss the point, but you said that the derived solidification of $G(\underline{X})$ is a singular complex, right? So it is a quasi-isomorphism? 

%Ah, quasi-isomorphism. I see it is only a quasi-isomorphism. That does make sense.
% todo
% --> Maybe just deletes this question and move on.

\begin{sketch}
% 20:22
Again, there is the formal reduction to the case of finite CW complexes---just assuming I keep, which, \note{todo, edit text}

Formal reduction to case of finite CW complexes.

% 20:46
For a finite CW complex, how do we compute this? 
Assume $X$ is a compact and finite CW complex. We know this homology is finitely generated in each degree.

% 21:48
How do we compute the derived solidification? We do the usual things. 
% 21:54
We take some surjection from some $S \in \Pro_\N(\Fin)$ onto $X$, like the Cantor set. 
\be 
S \epi X
\ee

Then we get all fiber products. 

% 22:12
This gives a resolution of this condensed sheaf by the constant sheaves, and then the constant sheaves on the fiber products, which are again some profinite sets, and then all the fiber products.
\be 
\ldots \to \Z[S \times S] \to \Z[\underline{S}] \to \Z[\underline{X}] \to 0.
\ee
% todo

% 
So that is the resolution of this guy. To form the derived solidification, we can apply the solidification to all the terms here. But for these terms, we know that it is concentrated in degree zero. 
% 22:45
We know that the derived solidification is computed by this, let me not underline.

So $\Z[\underline{X}]^{L\solid}$ is computed by 
\be 
\ldots \to \Z[S \times_X S]^\solid \to \Z[S]^\solid \to 0.
\ee

% 23:16
This might seem a bit hard to compute, but we know that each term (like $\Z[S]^\solid$) here is $\IntHom(\Cont(S, \Z), \Z)$, the internal $\Hom$ from the continuous functions from $S$ into $\Z$, back into $\Z$. That is something I discussed at the end of the last lecture. 
% 23:32
This formula that this limit of the $S_n$ can be translated into this formula. 
% 23:36
So these are some kind of measures on $S$, and similarly for the other guy.

\end{sketch}

% 24:17
This means that all the terms in this complex are isomorphic to their double dual. 
% 24:24
The continuous functions $\Cont(S,\Z)$ is the dual of this one, and then I dualize again. The solidification is just $\Hom$, and I could also put the $\R$ here, it does not change in those cases.
\be
\Z[S]^\solid \iso \IntRHom(\IntRHom(\Z[S], \Z), \Z)
\ee

% 25:00
This means that the same formula will be true for $\Z[\underline{X}]$ \note{not sure if $\Z[\underline{X}]$ or $\Z[S]^\solid$ is meant}, if you think about it. Because it is a resolution, this means that also this derived solidification here, one way to compute it, is that it is isomorphic to $R\Gamma(X, \R)$.
\be 
\implies \Z[X]^{L\solid} \iso \IntRHom(\IntRHom(\Z[\underline{X}], \Z), \Z)
\ee

% 25:31
But now we are in business, because we know that $\IntRHom(\Z[\underline{X}], \Z)$ is isomorphic to the singular cochain complex.

%todo brakets
$C^\ast_\sing (X, \Z)$

% 25:45
Because we know that the singular cohomology for finite CW complexes is finite in each degree, it is taking the dual of singular cohomology against singular homology.

% todo brackets
\begin{align*} 
C^\sing (X,\Z) \text{as all $H^\sing_i (X,\Z)$ are finitely generated}
\end{align*}

% 26:20
So you get that the derived solidification of $X$ is the dual of singular homology. 

% 26:40
This means that to some extent, this passage from light condensed abelian groups or solid abelian groups is like passing to homotopy types. At least for CW complexes, it contracts the interval and then identifies two homotopy equivalent CW complexes. 
% 27:00
But on totally disconnected things, it is much finer information. 
% 27:06
You could still invert homotopy equivalences, but not invert weak homotopy equivalences.

% 
\ques Is it the case for locally contractible spaces, in the sense that you explained? 

\ans Yes, whatever I said about CW complexes also works for a locally contractible space. Zhouhan \note{not sure of the name} pointed out to me that the paracompactness assumption is actually not required in comparing singular cohomology and sheaf cohomology. 

\ques No? 

\ans No, they are not required. There are some recent papers to that effect.

\subsection{Understanding the structure of $\Solid$}

% 28:40 
Now let me go back and try to describe the category of solid abelian groups a little better. We want to understand the structure of solid abelian groups better.

\bit
	\item finitely generated objects: quotients of $\prod_\N \Z$
	\item finitely presented objects: cokernels of a map 
	\begin{align*} 
	\prod_\N \Z \to \prod_\N \Z
	\end{align*}
\eit

% 29:10
So there is a compact projective generator. These are precisely the quotients of the products of these $\prod_\N \Z$.

% 29:27
Then there is a notion of finitely presented objects. These are precisely the cokernels of a map.

% 29:50
It turns out that solid $\Z$-modules behave like modules over a completed  ring in the following sense. They are finitely presented objects, forming themselves an abelian category. The only critical thing is stability under kernels. 
But once you have that, it is automatically stable under kernels, cokernels, and extensions. Really, only the kernels are something to mention.

% 31:00
The whole category is just the $\Ind$-category of those. 

% 30:15, todo proper placement
\begin{theorem}
The finitely presented objects of $\Solid$ form an abelian category, stable under kernels (!), cokernels and extensions.
\be
\Solid \iso \Ind(\Solid^{\text{finitely presented}})
\ee

\end{theorem}

% 31:20
Also, any finitely presented object is actually isomorphic to a cokernel of an injective map. This is something slightly better than what Dustin announced in the first lecture. 
He claimed that the finitely presented objects have a resolution of length two, but actually length one is good enough.

% 32:12
\bl \label{lemma:06-fin-pres}
Any finitely presented $M \in \Solid$ has a resolution that just stops.
\be
0 \to \prod_\N \Z \to \prod_\N \Z \to M \to 0
\ee
\el

So there are some finite objects in your category which are exactly the cokernels of injective maps. Then everything is a kernel of a map \note{not sure of this} from one of those.

% 32:56
The key lemma for this is the following. We are trying to understand all the finitely presented ones. You have a kernel. 
You can always find a surjection onto $M$, and then there is some kernel. The kernel is at least a finitely generated submodule of this. Then it would be good to know that all the finitely generated submodules of this are actually such a product. This is what I claim now.

% 33:34
% Key lemma
\bl \label{lemma:06-fin-generated}
Any finitely generated submodule of $\prod_\N \Z$ is isomorphic to a product of copies of $\Z$.
\el

% 34:18
\ref{lemma:06-fin-generated} definitely implies the statement in \ref{lemma:06-fin-pres}, but it also implies... So objects are always stable under cokernels and extensions. The critical thing is that they are stable under kernels. 
% 34:32
But then \ref{lemma:06-fin-generated} also always reduces to identifying the finitely generated submodules of your generating objects. If those are all finitely presented, then you are in good shape.

This theorem is easy to deduce from this lemma. So let's prove this lemma.

% 35:03
\bq
We know that it is finitely generated, so we know that there is some surjective map from a product of copies of $\Z$ onto $M$ which injects back into $\prod_\N \Z$.
\be
\prod_\N \Z \epi M \mono \prod_\N \Z
\ee

By the way, this is again the result... Previously, a lot of what I was talking about, about the general properties of the category of solid abelian groups, they extend to the full condensed setting, not just the light one.
% 35:33
This theorem is again one which only holds in the light setting. Because we are using some countability again in just a second.

% 35:50
So we have some map here. Let's call it $g$.
% todo: draw map g

We know that the maps from a product back to $\Z$, they are given by direct sum. So we know that this is dual to a map in the other direction from a direct sum of these. 
Our task is to show that whenever we have such an endomorphism of a direct sum of $\Z$, and when we dualize, then this image here ($M$) is itself a product.

So:
$g$ is dual to a map
\be
\bigoplus_\N \Z \xrighthook{h} \bigoplus_\N \Z \to
\ee

% 37:12
For the proof, I will use the following fact, which I am not sure if it is well-known.
% 37:24
\underline{Fact:} Let $N$ be a countable abelian group that embeds into a product of copies of $\Z$. In abstract, $N$ is discrete, there is no condensed structure. Then $N$ is free.

% 38:16
Let me leave this as an exercise. I know it is definitely false if you drop the countability assumption, because if you take an arbitrary product of copies of an abstract abelian group, it is definitely not a free abelian group.

% 38:35
But one thing this implies is that if I look at the image of $h$, then this is definitely countable, and it embeds in the direct sum and also the direct product of these \note{not sure of this}.
This also embeds into the direct sum, 

This implies that the image of the map is free.
\begin{align*}
\implies \Im(h) \text{ is free}
\end{align*}

% 39:10
The image is free, but then you have a surjection here \ref{lemma:06-fin-generated} from this direct sum $\bigoplus_\N \Z$ onto another free group. 
So this splits. In particular, there is a kernel here, but this must split back. So the image of the map splits as a direct summand.

% todo diagram seq
\begin{align*}
\implies \ker(h) \text{ splits as a summand}
\end{align*}

% 39:50
So the kernel splits, but this means that you can replace this $\bigoplus_\N Z$ by the other direct factor, which is also a free abelian group.

% 
Freyd and replace $H$ by the corresponding thing, because then this corresponding product will split into two copies, and this $N$ will just embed into one of them. 
So we can definitely assume that $H$ is indexed.
\begin{align*}
\leadsto \text{You can assume w.l.o.g. that $h$ is injective}
\end{align*}

% 40:27
Now we have an injection.
% 40:30
\be
0 \to \bigoplus_\N \Z \hookrightarrow \bigoplus_\N \Z \to Q \to 0 % todo h
\ee

Now we have some quotient here. The next task is to understand the structure of this quotient.

% 39:05
% For this, you do not need this [previous fact]. It is just the image. Okay, I will use this [previous fact] again in a second, so I wanted to mention it.

% 
Maybe this [previous discussion] is completely irrelevant. Maybe I should focus more on the question at hand. 
But let me try to understand a little bit about the structure of such maps and what it implies on the dual side.

% 41:34
There is a certain quotient of $Q$, which is the image when you embed it into a doublde... Let me write it as a product over all possible maps from $Q$ into $\Z$. 
\bee
0 \to Q^{'} \to Q \epi \overline{Q} \mono \prod_{\Hom(Q,\Z)} \Z
\ee

% 42:02
So you can look at all possible maps that $Q$ maps to a free abelian group, or just to $\Z$, and take the corresponding maps to $\Z$. 
% 42:10
Then there will be some quotient here. By the fact that $\overline{Q}$ is free, this means free or in particular projective, so it splits as a direct summand.
% todo add to eq
\eq

% 42:40
Also, if you look at the kernel of this map, then it is easy to see from this that this $Q^{'}$ will have no more maps to $\Z$.
\be
\Hom(Q^{'},\Z) = 0
\ee
Because if this had a map to $\Z$, then because this is a direct sum, there is another map from $Q$ to $\Z$, which would make $\overline{Q}$ larger. 
This means that this has this quotient $\overline{Q}$ which is free, and so this splits back here, but then also back here if you want. 
Then you can also get rid of that summand.
% todo diagram



% 43:20
Without loss of generality, you can replace $Q$ by $Q^{'}$, and then there are no more homomorphisms to $\Z$ \note{todo}. Those are without loss of generality. $Q$ has no more maps to $\Z$.

\be
\text{w.l.o.g. } \Hom(Q,\Z) = 0
\ee

% 43:40
A different way to think about what I am doing here, is to observe that the category of countably generated free abelian groups has cokernels. This $\overline{Q}$ would be the cokernel.

% 44:38
So where are we in the proof?
We are trying to show that whenever we have a map from a direct sum of $\Z$, once we dualize, the image of this map is itself a product of copies of $\Z$. 
Now we split off the kernel, we split off part of the cokernel, and now we are down to a situation where we have this map and $Q$ has no more maps to $\Z$. We are trying to understand what happens if we dualize.

Dualize $h$

% 45:13
So we have here a product of copies of $\Z$. This map here is our old map $D$. 

\be
0 \leftarrow \IntExt^^1(\Q,\Z) \leftarrow \prod_\N \Z \leftarrow \IntHom(\Q, \Z) \leftarrow 0
\ee
% todo

% 45:46
The kernel of this map is precisely the $\IntHom$ from $Q$ to $\Z$. Then the kernel would be $\IntExt^1(\Q,\Z)$.

% 46:20
Now, we ensured that the $\Hom$ from $Q$ to $\Z$ is zero, but it turns out that the $\IntHom$ from $Q$ to $\Z$ is zero. 
The points of $\Hom$ from $Q$ to $\Z$ join into $\Z$, which by using the adjunction in a different order, is the $\Hom$ from $Q$ into the continuous homomorphisms from $\Z$ to $\Z$, which is a discrete guy and where this guy is solid. 
% todo

\be
\Hom(Q \tnsr \Z[S], \Z) = \Hom(\Q, \Cont(S,\Z)) = 0
\ee
%todo brackets under

% 
But $Q$ has no maps to $\Z$ and just no maps to any abelian group. 
% 47:18
So what does this mean? 
This means that the image of $g$ is this product of $g$'s \note{not sure}. So some of the reductions we made in the beginning precisely ensured that our map $g$ became injective.

Thus, $g$ injective so $\Im(g) \iso \prod_\N \Z$

% 47:47
Strictly speaking, when I did some of these reductions, it could have happened that an infinite direct sum became a finite direct sum. 
But just take the direct sum with an infinite thing, whatever.

% end proof

% 

% 48:25
Corrolary of the proof is:
Any $M \in \Solid^{fin. presented}$ is product of copies of $\Z$ and the group of the form $\Ext^1(Q, \Z)$ for some countable abelian group $Q$, $\Hom(Q,\Z) = 0$

The light condensed rings are true again, which is also the reason that we never saw any obstruction to this in actual mathematical practice.

% 49:53
So $Q$ is actually a torsion group. These are precisely the profinite ...
by Poincare duality.
In general there are some kind of weird non quasiseparated guys.

Here is another corollary.

% todo, right place
% 50:30
\bc
$\prod_\N \Z$ is flat for $\tnsr^\solid$.
\ec

Again, this corollary is something that is only true in the light setting, namely that the product of copies of $\Z$ is actually flat.

% 50:55
So in abstract, symmetric monoidal category it is always true that projective objects are exact.
* 51:00
We could check that once we pass to the $p$-adic numbers, something like this holds true in, like without the lightness assumption. But we were kind of confused about the case of solid abelian groups. But then [name] \note{todo} observed that this flatness is actually false in all solid abelian groups.

% 51:21
But once you restrict to the light condensed abelian groups it becomes true again. This is also kind of the reason that we never saw any obstruction to this in actual mathematicaly pracice.
% 51:38
Why? 

\bq
Need: $\forall M \solid$, $M \tnsr^{L\solid} \prod_\N \Z$ sits in degree 0.
\eq

% 
We need for all solid $\Z$-modules, the product is solid. So without loss of generality, we actually have a resolution of length one. 
% todo

\be
0 \to \prod_\N \Z \to \prod_\N \Z \to M \to 0
\ee

% 52:29
But now, if I tensor this with a product of copies of $\Z$ ($\tnsr^\solid \prod_\N \Z$), then this map stays injective. 
% 52:50
What you see, is that if you take the derived tensor product of this, you get a product of copies of $M$, because you can write this as a finitely presented guy.

\be
M \tnsr^\solid \prod_\N \Z = \prod_\N M
\ee

% 54:28
One thing that is really nice about this solid tensor product is that many computations come out right, sometimes in non-obvious ways. 
Just like previously, a lot of $\Ext$ computations came out right in non-obvious ways.

% 54:50
\subsection{Some $\tnsr^\solid$-computations}

% 54:48
Let's do some tensor computations. Often, you maybe care about things like: you start with some $M$ being a discrete abelian group. Then you form the $p$-adic completion of $M$.
In such situations, one often uses the completed tensor product $\hat{\tnsr$, which is like the completion of the usual tensor product, if you do some formal geometry or something.


Let $M$ be an abelian group. $M^\ast_p = \lim_n M / p$ % todo
In such situations, one often uses $\hat{\tnsr} = $ $p$-adic completion of ... $\tnsr$

% 56:10
Here is a theorem that is actually what the solid tensor product does. Actually, in full generality, for any abelian group, it is better to replace this by the solid derived $p$-completion, which is the derived $p$-completion operation. 
Here, this derived thing just means a complex represented by multiplication by $p$, where this sits in degree zero and this sits in homological degree minus one or homological degree one. % todo
% 
Almost always, this agrees with the usual $p$-adic completion in degree zero, but in complete generality, that is a better operation.

% 57:00
Generally, it makes sense to talk about derived completions---these things are the derived complete modules. 

% 57:12
Here is a proposition, and because we are doing this, it is slightly better to work from the start in the derived category. 
Let me assume I have things which are concentrated in non-negative degrees, so things go to the left.

\bp
If $N, M \in D_{>0}(\Solid)$ are derived $p$-complete, then $N \tnsr^{L\solid} M$ is derived $p$-complete.
\ep

% 
Say I have two of them, $M$ and $N$, and they are derived $p$-complete. So there are isomorphic to the derived limit of the derived ... $p^m$.  
%$p$-adic completion of $M$ tensored with $\Z/p^n$ over $n$. 
It is also a condition you can check on homotopy groups.

%  58:14
Then also their solid tensor product is the derived $p$-completion of their usual tensor product. 


% 58:35
Let me prove this in a second, but let me note one corollary, which is, for example, that if you take an infinite direct sum of copies of $\Z$, take the $p$-adic completion of that, and then take the tensor product of that with such a $p$-adic completion, you just get the similar $p$-adic completion.

\bc
\be
% todo wedge
(\bigoplus_\N \Z)_p \tnsr^{L\solid} (\bigoplus_\N \Z)_p \simeq (\bigoplus_{\N \times \N} Z)_p
\ee


\ec

% 59:05
This means that in such situations of more or less formal geometry, for derived $p$-adically complete things, the solid tensor product does what you would expect it to do. 
There is nothing special about the number $p$ here. You could work with any base ring and solid module structures over that ring, and then any element of that ring and it would still be true.

% 
So this $p$-adic completion of the free abelian group, you consider it as a solid abelian group, or as a condensed abelian group. 
It is solid because the class of solid modules is stable under all limits, and so on. 
You start with something solid like $\Z$, take a direct sum---it is solid. Take the $p$-adic completion---it is solid. Take a limit, and so on.

% 1:00:25
\br
There is nothing special about $p$. For any ring $A$ and any elements $f_1, \ldots, f_n \in R$, and $M, N \in D_{\geq 0}(\Solid_A)$ which are derived $x$-complete, then the derived tensor product $M \tnsr^{L\solid}$ of $M$ and $N$ is the derived $x$-completion of their usual tensor product.
\er

% 1:02:31
I should mention this is a corollary of the preceding proposition. We know there is definitely a map from $(\bigoplus_\N \Z)_p \tnsr^{L\solid} (\bigoplus_\N \Z)_p$ to $(\bigoplus_{\N \times \N} Z)_p$, because the tensor product linear maps, and so there is this obvious bilinear map.
But those sides are derived $p$-complete. By definition proposition
% todo wedges
% 1:02:52
To check whether it is an isomorphism, we can actually check after reduction mod $p$. Because for derived completed things, this isomorphism can be checked modulo $p$. 
But modulo $p$, both sides just become the tensor product, and you are just taking the usual tensor product of FP's and product, a direct sum of FP's, and a direct sum of FP's.


\begin{sketch}

% 1:03:45
Let me give a sketch. First of all, in this situation you can work with $\Z_p$ instead, because the derived completion of $\Z_p$ with respect to $p$ is just $\Z_p$. 
That is because you can take $\Z[[T]]$, $\Z$ power series in $T$, and then you quotient by $T-p$. mod $p$, you get here $\F_p[[T]]$, but also when you take this power series in two variables and quotient out by $p$, then you get $\Z_p[[T]]$ quotient by $p\Z_p[[T]]$. 
So this means that the derived category of solid $\Z_p$-modules is automatically a full subcategory of solid $\Z$-modules. So being a $\Z_p$-module is not a structure on a solid, it is just a condition. If everything is derived $p$-complete, then everything becomes a $\Z_p$-module.

\be
\Z_p \tnsr^{L\solid} \Z_p = \Z_p
\ee

\begin{align*}
todo
\end{align*}

% 
So $M, N \in D(\Solid_{Z_p})$ is actually here, and everything is taking place in the subcategory. 

\be
\implies D(\Solid_{\Z_p} \subs D(\Solid) p^{f(n)} \Z_p
\ee

% 1:05:00
Let me do the case which is maybe the most destructive case, where $M$ is just, take a direct sum of copies of $\Z_p$, but you complete the direct sum. 
Then in order to compute what the solid tensor product does, we have to write this in terms of our generating objects. 
% 1:05:33
This is a slightly non-trivial exercise, because there is a rather large collection of compact projective objects.

% 1:05:40
What is happening here, is that this is a colimit over all functions $f\cl \N \to \N$ which go to infinity. So they only take finitely many values below some constant, of $\prod_\N p^{f(n)} \Z_p$. 

Case $N = M$
$= \hat{\bigoplus_\N} \Z_p \simeq \colim_{f \cl \N \to \N \text{ going to $\infity$}} \prod_\N p^{f(n)} \Z_p $

To see surjective. Pick any $S \in \Pro_\N(\Fin)$ 

\end{sketch}

% 1:06:23
What is happening here? First of all, why is there a map? So whenever you have a map $f$ which goes to infinity, you can take the product of these copies of $\Z_p$ to the $f(n)$: $\prod_\N p^{f(n)} \Z_p$. This maps to the completed direct sum $\hat{\bigoplus_\N} \Z_p$, because modulo each power of $p$, almost all of the terms in this product $\prod_\N p^{f(n)} \Z_p$ will go to zero. 
That is because $f$ is going to infinity.

% 1:07:00
Quite obviously, this is also injective. But then you still have to show that it is a surjection. So you have to show that whenever a light profinite set maps into this completed direct sum $\hat{\bigoplus_\N} \Z_p$, it factors over one of these terms $\prod_\N p^{f(n)} \Z_p$.
% 1:07:12
But if you have a map into this completion, then pick any $S \in \Pro_\N(\Fin)$ and a map from $S$ to this copies of $\Z_p$, which is by the definition the limit. 

% 1:08:40
\be
S \to \bigoplus_\N \Z_p = \colim_n (\bigoplus_\N \Z / p^n )
\ee

% 1:08:55
% todo g
So we have $g$ here, and so we have a collection of $g_n$ here. 
Then each $g_n$ factors from some direct sum over integers at most some $a$ of $\Z / p^n$? % todo
Because $S$ is a compact object, and maps out of a compact object are finitely generated.

% 1:09:55
So there are just a bunch of elements where you get something mod $p^1$, and then there is a larger bunch where you get something nonzero mod $p^2$, and then there's something even larger where you get something nonzero mod $p^3$. 
% todo diagram
% 1:10:11
But then you can just find some slowly, very slowly increasing function $f$ which factors over this product. 
% 1:10:23
So what is my diagram here? These are my $m$, these are my $m_n$, and this is $f(1)$, this is $f(2)$.

% 1:11:21
So this means that this derived tensor product of $M$ and $N$, each of those, is a product of $\Z_p$.

% 1:12:55
Now this has an obvious map, so $f$ on it, over all functions. Let me call them $H$, which are now functions on both coordinates, which just go to infinity. 
Yes, $H$ is a function of two variables, and then you put the function of $H$ and $n$ here, where this is completed.

\be
= \colim_{h \cl \N \times \N \to \N} \prod_{ \in \N} \Z_p = \bigoplus_{\N \times \N} \Z_p
\ee
% to infty
% todo

% 
Now, at first you might think that this will surely not work out, because here you are allowed to quantify over all arbitrary slowly increasing functions of two variables, whereas here you are just getting those that are sums of functions of one variable individually. 
% 1:13:51
But then there is---maybe at first slightly surprising but not that hard to prove combinatorial lemma---that whenever you have such a slowly increasing function $H$, you can always find one that is even slower increasing, which is a sum of two functions of individual coordinates. 
% 1:14:05
I will leave it to you as an exercise to figure that out. 
But that is what I mean, that the softer approach for non-obvious reasons gives you the correct object.

% 1:15:20
The general argument is saying that you can reduce to the case that $n$ and $m$ are just some completed direct sums of copies of the generators. 
Then, although the same argument---you have to be slightly careful because a priori, you can also have the case that $m$ and $n$ are completions of a direct sum which is over an uncountable index set. Then you still want that.
% 1:15:50
But then there is an argument that the uncountable case, by more formal arguments, reduces to the countable case. Because these derived completions, they always commute with, I mean, you can always reduce uncountable things to countable things.

\subsection{Solid functional analysis}

% 
So this is a nice computation. There are some even further computations that come out, which really appear when you do some solid functional analysis. 
% 1:16:37
Let me work over $\Q_p$ for simplicity, but most of what I am saying works over any field.

% 
Again, the solid $p$-adic Banach spaces, or even the derived category, is just the full subcategory of $p$-modules, and those where $p$ is invertible. 
It is the full subcategory of abelian groups, and if you form tensor products and direct sums, it is in the subcategory.

\be
D(\Solid_{Q_p}) \subs D(\Solid_{Z_p}) \subs D(\Solid)
\ee

% 1:17:40
This category $\Solid_{Q_p}$ itself, it now has a compact projective generator $(\prod_\N Z_p)[\frac{1}{p}]$.
Here in $\Solid_{Z_p}$, it will be a product of $\Z_p$'s, and in $\Solid_{Q_p}$ you have to invert $p$, take $(\Z_p)^\vee$. 
% 1:17:50
This $(\prod_\N Z_p)[\frac{1}{p}]$ is a slightly curious topological vector space. (Or like a condensed vector space, but it comes from a topological vector space.)
% 1:17:58
So it looks like one where, somehow, the unit, which has a norm, where the unit ball is this thing \note{add what he points at}, where the unit ball is compact. 
But there is this general thing, that you cannot have Banach spaces or normed vector spaces of infinite dimension, where the unit ball is compact.


% 1:18:14
So it turns out that if you were trying to endow this with a norm in the obvious way, where this would become the unit ball, then the norm map would not be a continuous map. 
% 1:18:25
So this is not really a normed vector space. It is what it is.

% 1:18:33
These things like $(\prod_\N Z_p)[\frac{1}{p}]$ have appeared a little bit also in the classical functional analysis literature. Maybe first Lefschetz called them "Smith spaces". 
% 1:19:13
She was studying the analog of those things over the real numbers.
So these are $p$-adic Smith spaces. 
They can also consider some type of topological vector spaces, which are increasing unions, gotten by scaling out some compact convex set.

% 1:19:40
The more usual objects we consider in $p$-adic functional analysis are $p$-adic Banach spaces. At least separable examples of those are exactly the ones that we considered previously. 

So:
\begin{align*}
\text{more common objects:} \quad & (\bigoplus_\N \Z_p)^{}_p[\frac{1}{p}] \quad & \text{$p$-adic Banach spaces}
\end{align*}
\note{todo, notation in superscript in equation}
%
You take countable copies of $\Q_p$, complete them, and then these things are in reality Smith spaces. 
%
So either you put "separable" and "light" on those sides, or you... So "separable" on the Banach side means you can take a countable direct sum, and "light" means on the other side, you take a countable product. 


\bp

(light) Smith spaces $\simeq$ (separable) Banach spaces

\ep

%
This is just the obvious: you take a Smith space and dualize it, but you can also take Banach spaces like so. 
Yes, the generator that we use here, the $(\Z_p)^\vee$, is sometimes dual to Banach spaces. Okay, we are basing everything on them.

\br
Derived duality?

\be
V \mapsto R\IntHom(V, \Q_p) = \IntHom(V, \Q_p)
\ee
\er

% 1:22:05
Again, here isa curious remark, and maybe people will
Just the internal dual, that is true and obvious. But if you go the other way, you can wonder: if you take a Banach space and take the dual of that, is it just the usual dual? 
% 1:23:03
The answer is that it depends on your model of the set theory. In the same way as this came up before, it fails under the continuum hypothesis, but under this principle $\ast$ that I mentioned (that is consistent), it is true. 
Because a Banach space is a special type of pro-abelian group---a limit of direct sums over $\N$ of $\Q_p$-mod-$p^{t(n)} \Z_p$. 
So this duality between $\Pro$-things and $\Ind$-things is precisely the thing that is controlled by principle $\ast$.

% 1:24:28
Yes, the more I think about it, the more I am tempted to just use a set theory model where principle $\ast$ is true. Because you have the choice of either these higher $\Ext$ groups being some junk that has no meaning at all, or them being zero.

% 
Okay, but other than that---this question was never really relevant for what Dustin and I have been trying to do. Sometimes you run into this thing. 
It is definitely like, if you do some computations now within this solid functional analysis, you're doing lots of $\Hom$'s, lots of tensor products, lots of $\Hom$'s, and so on. 
At some point, surely you will take some $\RHom$ of a Banach space against something in $D$ somewhere. 
Then you can decide whether you work in a model where it is just some junk, or you work in this model where it is what it is supposed to be.

% 1:25:30
What I really wanted to say is, you also have Fréchet spaces, which are countable limits of banach spaces along compact maps. 

\begin{align*}
\text{Fréchet spaces} = \text{Countable limits of Banach spaces under dense transition maps}
\end{align*}

These really pop up a lot. There is a standard notion of a completed tensor product for them. 

have standard notion of $\tnsr$ todo for them, compatible with such limits.

In particular, it is the one that is compatible with limits. 
% 1:26:28
On Banach spaces, it is the usual Banach tensor product. When you have Fréchet spaces (in such a countable limit), then it is the corresponding limit of completed tensor products of Banach spaces. 
% 1:26:42
A general thing that usually happens when you define such tensor products in functional analysis, is that you are trying to make them compatible with limits. 
%
Which is not related, because our solid tensor product was defined to be compatible with colimits. 
Usually, there are very few functors that are compatible with both limits and colimits---certainly the tensor product is not compatible with \emph{all} limits. 
%
But fortunately, it turns out that in this situation, it does give the correct answer. So here is a proposition:

\bp
If $V, W$ are Fréchet $\Q_p$-vector spaces, then:
\be
\underline{V} \tnsr^{L\solid} \underline{W} \simeq V \hat{\tnsr} W
\ee
\ep

% 
If $C$, $M$ and $W$ are Fréchet spaces (considered as topological spaces for the moment), then you can pass to the condensed world and take the solid tensor product (or even the derived solid tensor product). It turns out that this is just the completed tensor product of Fréchet spaces. 
Then pass back. In particular, it is degree zero.

% 
For example, if you take a product of copies of $\Z_p$ and take another product of copies of $\Z_p$, it becomes the bi-infinite tensor product $\bigotimes_\Z^\infty \Z_p$. 

\bex
\be
\prod_\N \Q_p \tnsr^{L\solid} \prod_\N \Q_p \simeq \prod_{\N \times \N} \Q_p
\ee
\eex

This might seem like it is a standard thing on compact projective generators, but it is absolutely not. Because these things are very, very far from being compact projective---only these kind of unit balls in there are compact projective objects, but this is a huge space.

% 1:29:20
Again, let me just give a proof sketch, or an example. 

\begin{sketch}
\be
\prod_\N \Q_p = \colim_{f \cl \N \to \N} (\prod_\N p^{-f(n)} \Z_p )[\frac{1}{p}]
\ee

\be
\prod_\N \Q_p \tnsr^{L\solid} \prod_\N \Q_p = \colim_{f, g \cl \N \to \N}(\prod_{\N \times \N} p^{- f(n) - g(n)} \Z_p )[\frac{1}{p}]
\ee


\be
\colim_{h \cl \N \times \N \to \N} 
\ee

\end{sketch}

%
The general proof is, in some sense, combining the proof I did there with those points, with the one I will do here. 
Bhargav and I tried a minimalistic approach to combining these, but Sasha actually has a very fancy way to combine them that he needed for some categorical stuff.

%
How do we write this product of $\Z_p$'s? It is again such a huge projective limit. 
This time, you are taking such unit balls, but where you make the denominators larger and larger. 
So you take $\varprojlim_f \prod_{n \in \N} p^{-f(n)} \Z_p$, where again $f$ is some function from 

% 
$\Z_p$, and again, this is next to similar Schwartz functions, which are functions of both variables going to infinity very fast.

% 
Again, you might naively think that surely if I have two variables, I can build functions that are so fast-increasing that I can never dominate them by something that is a sum of functions in both variables. 
But it turns out, again by the same argument as there is, you can always dominate any such function by a function. That is fun.

% 1:33:00
I should maybe say, this is some filtered colimit here, but the precise structure of this index poset---this is actually where all the subtlety appears. 
%
Here we do not have to understand what this looks like, because this colimit you can just prove. 
%
But for these $X$ computations, you really have to be starting with such a description in the case of $B$ space and then do that. 
Then this huge colimit becomes a huge derived limit. Then you really have to grapple with the structure of this kind of poset of functions that grow arbitrarily fast. 
%
This is something that depends very much on your model of set theory, particularly on the cardinality of your set theory.

\subsection{Questions}

% 
I think that is all I wanted to say. But if there are questions, we have a few minutes.

% 
\ques For functional analysis in some very large Banach spaces, does it make a difference if you work in liquid condensed sets or all condensed sets?

% 
\ans It does not make a difference, because all Banach spaces are liquid. All Fréchet spaces are liquid, no matter whether they are separable or not. 
It is just the Preditorials that are not factored.

% 
\ques Is the relation between the homology of the $C$ complex and solidification kind of an accident, or should I take it as something deep? Can I expect that we will have another example, in condensed spectra or something like that, for other cases?

% 
\ans Instead of taking the de Rham $C$ being groups, you could work everywhere in a category of spectra. Then you could also... 
Maybe a good point to point out is the following: there is a spectrum called $ku$, which I like to think of as taking the algebraic K-theory of the complex numbers, but you take into account the topology that the $\GL_n(\C)$ has. 
This makes it homotopy-theoretic. Classically, this is a little bit hard to phrase what this is supposed to mean. I believe there are ways to do it anyway.

% 
One way you can do it in our formalism is you can take the K-theory of the complex numbers as a condensed spectrum. 
Because the complex numbers, this is a condensed ring. 
This means that the K-theory series has some automatic structure as a condensed spectrum. 
Because to give a condensed spectrum, I have to give a function where for each profinite set $S$, I have to produce a spectrum. But I can just take the K-theory of the continuous functions from $S$ to $\C$.

% 
So far, this is not at all true, because here the $\pi_1$, for example, will be $\C^*$ as a condensed being group. 
But now you can take $K(\C)$ and solidify. Just like we had solid sheaves of solid being groups and sheaves of condensed being groups, you also have solid spectra inside of all condensed spectra. 
This solidification does exactly what you wanted to do. It somehow contracts all the $GL_n(\C)$ that this was built out of, topologically, essentially by the proposition I mentioned in the lecture today.


\begin{align*}
ku \neq K(\C)
\end{align*}
% todo annotations

\begin{align*}
ku = K(\C)^{L\solid}
\end{align*}

% 1:38:40
Then you see that this is exactly the... You could do the same thing not just for the complex numbers, but for any algebra over the complex numbers, or even a category over the complex numbers. 
Then take the K-theory as a condensed spectrum and solidify. 
This gives a construction of what is known as a semi-topological K-theory. So solidification is what makes semi-topological K-theory.

% 
If $A$ is any kind of $\C$-algebra with a topology (I do not care about the specific topology), then you can similarly take the K-theory of $A$ and solidify. 
This recovers something that is known as the semi-topological K.
Think: can you get a similar result for $K_O$ or $KR$? Sure, just write $\R$. I mean, all the obvious variations. 
So in particular, $K_O$ is $K(C^*(\R))$. There is something $KU^{top}$ or whatever it is, and then maybe related to the...


If $A$ any $\C$-algebra:

\begin{align*}
K(A)^{L\solid} = \text{"semitopological $K$-theory"}
\end{align*}

% 
Here the $K$ is the periodic (or the full) spectra. In this notation, the $K$-theory spectrum... 
It turns out to be connective for the inputs that I am using, maybe not quite obviously so, but it is true. So there is a connective $KU$. 
It is much more subtle to recover the periodic $KU$. You can of course just invert both at the end, but...

% 
Sometimes, in the $p$-adic case, the stuff that Dustin Clausen and I developed, which is a different way to approach this kind of stuff, is aimed at defining some version of $C^*(\C)$, but now an analytic ring, where the answer certainly has homotopy in negative degrees.

% 1:42:15
\ques Is it somehow related to the description of the small $KU$ or small $K(\R)$ as some configuration space-like description given by Segal?

% 
\ans I do not know about the... Sorry, there is some description for small $K(\R)$ and for $KU$ using a thing given by Segal. 
I think this statement is much more naive. This statement is really just one way of encoding the intuition that... 
Or maybe see, but in some sense this is false. 
Something that is true before group completion, namely that if you take the infinity groupoid classifying space of $\C^\times$ and then pass to homotopy types here, then $KU$ is a group completion of that, and solidification factors over passing to homotopy types. 
So this is Segal's...

% 
But they have, I do not think, any configuration spaces.

\ques To what extent does the solid tensor product combine two topologies? 
Say we have $M$ and $N$, and $M$ is $x$-complete and $N$ is $y$-complete. 
There was a thing that I mentioned: if you take power series $\Z[[T]]$ and then $\Z[[U]]$, then $\Z[[T]] \tnsr \Z[[U]]$ becomes the $(U,T)$-adic one. 
So there it does do this thing.

In this more general condensed setting, it does not do it. So when you $-\tnsr_A-$ and you have this $x$-completeness statement, now when you complete the ring $A$, this one is no longer the derived completion. 
Does it make a difference for the proof?

Sorry, I realized that probably the most general version for solidification might not be expected to be true. 
I was getting confused about this. If you want to compute some product of a solid thing or whatever, one way is to kind of resolve by the absolute one. 
Then you are also taking a product over $A$, and so to show that this here is the $xy$-completed one, it is enough to know that all the other ones are. 
This way you can reduce to the case that the ring $A$ is just $\Z[[x]]$ here. 
Then the derived $x$-completeness of this one reduces to the one of $\Z[[x]] \tnsr \Z[[y]]$ here. But $A$ itself is derived $x$-complete. 
If it is $x$-complete, it is also derived $x$-complete. 
So you can reduce this statement to the case that the ring is really just the join $\Z[[x]]$.

\ques What did you mean by "passing to homotopy type" in $B(GL_n(\C))$? 
So $\GL_n(\C)$ is viewed as a topological space, and therefore as a condensed set or abelian group?

Yes. So when I define $K(\C)$, one way to do that is to take this classifying space
\end{unfinished}