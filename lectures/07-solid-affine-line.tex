% !TeX root = ../AnalyticStacks.tex

\section{\ufs The solid affine line (Clausen)}

\url{https://www.youtube.com/watch?v=fUjn2rGw9SA&list=PLx5f8IelFRgGmu6gmL-Kf_Rl_6Mm7juZO}
\renewcommand{\yt}[2]{\href{https://www.youtube.com/watch?v=fUjn2rGw9SA&list=PLx5f8IelFRgGmu6gmL-Kf_Rl_6Mm7juZO&t=#1}{#2}}
\vspace{1em}

Today we are going to be talking about a little bit of geometry, namely the solid affine line. Let me start with a recap of what we have seen so far.

\subsection{Recap}

We had this category $\Solid_\Z$ of solid abelian groups, which was a full subcategory of light condensed abelian groups. 
\begin{align*}
\Solid_\Z \subseteq \Cond_\Z^\light
\end{align*}

This $\Solid_\Z$ was an analog of complete non-archimedean topological abelian groups, but from a formal perspective, it is easier to work with. It is an abelian category, closed under limits, colimits, extensions, and many other things besides. 
% 1:30
Also, we had this left adjoint $(\ldots)^\solid$ called solidification. 

\note{todo, equation for left adjoint}

There is a symmetric monoidal structure here, which we can think of as a completed tensor product, such that this completion functor is symmetric monoidal.

% 1:50
The definition was in terms of this object $P$, which was the free module on a nullsequence. 
\begin{align*}
P = \Z[\N \cup \infty] / \Z(\infty)
\end{align*}

% 2:00
The definition was that $M$ is solid if this map from nullsequences to nullsequences given by $\id - \sigma$ is an isomorphism. ($\id - \sigma$ here is a shift on $\N$.)
\begin{align*}
M \text{solid} \iff \IntHom(P,M) \xrightarrow[\iso]{\id - \sigma} \IntHom(P,M)
\end{align*}

% 2:54
\ques There are two shifts one can look at, because you can shift one to the left and one to the right. Both equivalent? \note{Can't hear this question well.}

% 3:03
\ans I think only one is good. I want the one that is injective.

\subsection{Geometry of the affine line}

So that is what we had last time.

% 2:35
Now I want to start doing a little bit of geometry. We are going to be modest and look at the affine line, which is the most important case. It turns out that $P$ is a ring, even before solidification. 

% 3:10
$P$ is a ring, and there exists a ring map. So this is the polynomial ring in one variable to $P$.
\begin{align*} \label{07:poly-ring}
\Z[T] \to P
\end{align*}

The shift is multiplication by $T$. 

% 3:30
That is what you had have before solidification. But after solidification of \ref{07:poly-ring}, something extremely nice happens.

% 3:38
We already explained what the free module on $P$ is. There is a compact projective generator here on $\Solid_\Z$, which is a countable product of copies of $\Z$.

$\text{compact projective generator} = \prod_\N \Z$

% 3:55
That is the basic object from which everything is built. You can get that object by solidifying this basic sequence space $P$. 
If you take into account the ring structure, when you solidify \ref{07:poly-ring}, you just get the power series ring in one variable. 
\begin{align*} \label{07:power-series-ring}
\Z[T] \to \Z[[T]] 
\end{align*}
\note{not sure of this notation $[[]]$}

% 4:18
So in the solid context, the universal carrier of a nullsequence is just this power series ring $\Z[[T]]$.

% 4:30
But moreover, there is a very nice property of this situation, given by the following lemma. 


% 4:33
\ques How do you multiply things in $P$ that look like a quotient, by something which is not... The nullsequences is like a ring without unit usually.

% 4:45
\ans Yes. Let me briefly explain how you get the ring structure. It is better to think of this construction as being attached to $\N$, rather than $\N \cup \infty$. More generally, if you have any countable set, you can define measures on it as the free module on any compactification, modulo the free module on the boundary. It is independent of the compactification, functorial for proper maps and symmetric monoidal. You can see that using independence of compactification. If you take the product of two countable sets, then measures on set $A$ tensor measures on set $B$ is measures on the product of set $A$ and set $B$.
% 5:26
So using that, and the fact that addition on $\N$ is a proper map that has finite fibers, you can see that addition on $\N$ induces this ring structure on $P$. Addition on natural numbers also induces this. \note{Does he point at $\Z[T]$, or the map?}


% 6:05
If we do the tensoring in the solid world of this power series ring $\Z[[T]]$ with itself, over the polynomial ring $\Z[T]$, we get the power series ring $\Z[[T]]$ again.
\bl \label{07:iso}
\be
\Z[[T]] \tnsr_{\Z[T]}^{L\solid} \Z[[T]] \iso \Z[[T]]
\ee
\el

The multiplication map is an isomorphism, we could say, even the derived tensor product.

% 6:25
This is quite elementary, because we know how to do tensor products in $\Solid_\Z$. Peter gave lots of examples of calculations in this category, and the tensor product of two of these basic objects is just another one of them. 

% 6:45
\begin{sketch}
If we do $\Z[[T_1]] \tnsr_{\Z}^{\solid} \Z[[T_2]]$, that is $\Z[[T_1, T_2]]$ by the basic calculation of the solid tensor product. Then if we want to get to this \note{add where he points at, todo: equation}, we just mod out by identifying the two different variables $T_1 - T_2$, and the power series ring modulo $T_1 - T_2$ is the power series ring in one variable.

% 7:19
There is another perspective. Peter also mentioned this fact that if we take the solid tensor product of two derived complete objects, then the result is still derived complete. 
% 7:32
Maybe we have to worry a little bit about this, but morally, if we want to check that \ref{07:iso} is an isomorphism, both sides are derived $T$-complete. We can check it modulo $T$, and modulo $T$ it is a triviality.
\end{sketch}

% 7:46
\ques Is the derived solid tensor product defined as the derived $T$-product, and then you solidify? How is it defined exactly?

% 8:00
\ans You can either say it is the left derived functor of the solid tensor product, or you can say that you take the derived tensor product and then derived solidify it. It is all the same.

% 8:15
\ques Do you need to know there are somehow enough flat objects to construct the---

% 8:23
\ans You do not need to know that. But it is true that all of these objects are flat. I do not know, did Peter mention that? I forgot.

So a priori, you need to resolve both variables, but since you have flat objects, you only need to resolve one.

% 8:47
Okay. So what is the interpretation of \ref{07:power-series-ring}? This $\Z[T]$ corresponds to the affine line $\A^1$, we could say. 
% 8:57
Then, since this \ref{07:iso} satisfies this idempotence property---sort of like when we invert an element in a ring, we get the same thing. We can think of this $\Z[[T]]$ as corresponding to some subspace $\A^1$. 

% 9:13
So a \emph{subspace}, as opposed to just a random space with a map to the affine line. 
That is the interpretation of this idempotency.

% 9:21
Then we could ask: how should one think of this subspace? What subspace is this? The naive thing to say would be that it is the formal completion of $\A^1$ at the origin, because it is a power series ring. 
% 9:35
But that naive interpretation is not the correct one.

So $\Z[[T]]$ does not correspond to a formal neighborhood of 0 in $\A^1$. 

The reason is that that interpretation is not stable under base change.

% 10:00
To look at base change, by which I mean, this is all implicitly over $\Z$, but we could tensor it to any ring, or any solid ring, and see what pops up. Let me do a little calculation.

% 10:17
Let's base change to a non-archimedean field. I will just take the simplest example, $\Q_p$. We will see what pops up. 
% 10:26
We take $\Q_p$ and then we do a solid tensor product with $\Z[[T]]$, and we want to compare this to $\Q_p[T]$.
\begin{align*}
\Q_p \tnsr^\solid_\Z \Z[[T]]
\end{align*}

% todo
%
So $\Q_p$ is $\Z_p$ with $p$ inverted, and the solid tensor product commutes with colimits in both variables. So this is the same thing as $\Z_p$ solid tensor product $\Z[[T]]$, and then we invert $p$.

\begin{align*}
\Q_p \tnsr^\solid_\Z \Z[[T]] = (\Z_p \tnsr \Z[[T]])[\frac{1}{p}]
\end{align*}

% 11:05
But this $\Z_p$ has a resolution by two copies of---so $\Z[[U]]/(U-p)$, it has a resolution by two copies of this power series ring. So we know how to do these tensor products, and it just does the naive thing of pulling in the limits.

% 11:28
The result of that is that we get $\Z_p[[T]]$, and then invert $p$ on the outside, which is not the same object as the formal completion at the origin, namely $\Q_p[[T]]$. It is contained in there, and it is the subset where the coefficients are bounded in $p$-adic norm.

\begin{align*}
= \Z_p [[T]][\frac{1}{p}] \neq \Q_p[[T]]
\end{align*}

\begin{align*}
= \Z_p [[T]][\frac{1}{p}] \subseteq \Q_p[[T]]
\end{align*}

% 12:06
What is the interpretation then of this ring $\Z_p [[T]][\frac{1}{p}]$? This ring is the ring of bounded functions on the open unit disk in $\A^1_{\Q_p}$.

% 12:36
After an arbitrary non-archimedean field extension of $\Q_p$, we could ask: when we plug in a value in that field $T$ of $\Z[[T]][\frac{1}{p}]$, when will such a series converge? The answer is, it will converge if and only if the number we plug in has absolute value strictly less than one. 

% 12:52 
\gabber It is the ring of \emph{bounded} holomorphic functions on the open unit disk. Not the full, because sometimes, there is another one. \note{not sure of the last part of this sentence}

\clausen That is true, that is the bounded one. Thank you. 

% 13:03
\gabber Sometimes people use the all kinds of things...

% 13:10
\clausen Yes.

\gabber I do not know...

\clausen Certainly, all these functions converge on the open unit disk, but they do not converge past that. 

% Unit disk is you can imagine putting the open unit disk at infinity instead. So then you are looking at the complement of the closed unit disk. The complement of a thing is just as good as describing the thing.

% 13:35
The interpretation that this suggests is that $\Z[[T]]$ should be thought of as corresponding to some sort of open unit disk. 

% 13:50
Over a discrete ring like $\Z$, we cannot tell the difference between the open unit disk and the formal completion at the origin. But the difference pops out when you make a base change to something that has more freedom to play with.

% 14:14
But in rigid geometry, it is not really the open unit disk that is fundamental. Usually, it is the closed unit disk. So what about the closed unit disk?

\subsection{Closed unit disk}

% 14:40
One way to understand the closed unit disk is, if we understand what an open unit disk is, we can imagine putting the open unit disk at infinity instead. Then we get the complement of the closed unit disk. The complement of the unit disk is just as good as describing the unit disk.

% 15:00
So let's take this object and put it at infinity instead. This is all just at the level of loose thinking. We can take this power series ring $\Z[[T^{-1}]]$ in one variable and tensor it over... but let me call the variable $T^{-1}$ instead, because I want to be thinking of putting it at infinity. Then I tensor over $\Z[T^{-1}]$ with $\Z[T, T^{-1}]$, so then I am puncturing at infinity, so that I actually  live in the affine line.

I have a homomorphism from $\Z[T]$... 

\begin{align*}
\Z[[T^{-1}]] \tnsr_{\Z[T^{-1}]} \Z[T, T^{-1}] \leftarrow \Z[T]
\end{align*}

% 15:41
What is this ring $\Z[[T^{-1}]]$? I am taking this power series ring and inverting the variable there. Another way of describing this is as the power series $\Z((T^{-1}))$. So that is the open unit disk centered at infinity.


% 16:17
If we want to understand the closed unit disk, we should in some sense be localizing away from this complement. Another way of saying that is that we should be killing this object $\Z((T^{-1}))$. 
So to get the closed unit disk, we need to kill $\Z((T^{-1}))$.

% 16:50
I am going to make a preliminary note. This $\Z((T^{-1}))$ is a module over $\Z[T]$, as I just exhibited, and it also has a very simple resolution.

% Note
\begin{align*}
\Z[[U]][T] / (UT - 1)
\end{align*}

Note that $\Z((T^{-1}))$---let's disambiguate and call the variable $U$---we can think of that as $\Z[[U]]$ and then invert $U$. One way to invert $U$ is to adjoin the object that you want to be the inverse, and then enforce the equation that says that they are inverse to each other. When we look at it like this...

I will rewrite that. We get $\Z((T^{-1}))$ has a two-term resolution, where you have $\Z[[U]][T]$ and $\Z[[U]][T]$. Here you have $U-1$, so it is a two-term resolution.
\begin{align*} \label{07:UT-map}
\Z[[U]][T] \xrightarrow{UT-1} \Z[[U]][T] \rightarrow \Z((T^{-1}))
\end{align*}

% 17:59
What are these objects? These are just the base changes of our fundamental $p$, not to $\Solid_{\Z}$ but to $\Solid_\Z[T]$. So these are resolutions by the compact projective generator of $\Mod_\Z[T](\Solid_\Z)$.

So it is a 2-term resolution by the compact projective generator of $\Mod_{\Z[T]}(\Solid_\Z)$.

% 18:40
Killing $\Z((T^{-1}))$ should be the same thing as requesting this map $UT-1$ in \ref{07:UT-map} to become an isomorphism.
% 18:45
Now this suggests the following, based on an analogy with the definition of $\Solid$ up there.
Let's say we have one of these objects. 

Let's say that $M$ is $\Z[T]$-solid if and only if when we take $\IntHom(P, M)$ and then $\IntHom(P, M)$, and then we take the map which is given by---so now $U$ corresponds to the shift, and $T$ is the extra thing we have acting on $M$, because $M$ is a $\Z[T]$-module, so we have $(\operatorname{shift}T - 1)$. We want this map to be an isomorphism.

\bd \label{def:07-Z-solid}
$M \in \Mod_{\Z[T]}(\Solid_\Z)$ is \textit{$\Z[T]$-solid} $\iff \IntHom(P,M) \xrightarrow{\sigma T - 1} \IntHom(P,M)$.

% 19:45
% todo: ref to note

$\iff \RHom_{\Z[T]}(\Z[[T^{-1}]],M) = 0$

\ed

Then this note up here is saying that that is the same thing as requesting that if --- now I should pass to $\RHom$. It is the same thing as requesting that there are no $IntRHom$s from this object we are trying to kill into $M$.

% 20:30
Now the theorem is that in this definition \ref{def:07-Z-solid}, the name \textit{$\Z[T]$-solid} is well chosen. This $\Z[T]$-solid theory over $\Z[T]$ is very similar to the $\Solid_\Z$ theory over $\Z$.

\bth

\begin{align*}
\Solid_{\Z[T]} \subs \Mod_{\Z[T]}(\Solid_\Z) \subs \Mod_{\Z[T]}(\Cond^\light_\Z)
\end{align*}

$\Solid_{\Z[T]}$:
\bit
	\item is an abelian category, closed under limits, colimits and extensions.
	\item if $M \in \Cond_{\Z[T]}$ and $N \in \Solid_{\Z[T]} \implies \Ext^i_{\Z[T]}(M,N) \in \Solid_{\Z[T]}$.
	\item $\exists$ left adjoint to inclusion, $(-)^{\T}^{\solid}$.
	\item $\exists$ symmetric monoidal structure on $(-)^{\T}^{\solid}$.
	\item The derived analog holds as well.
\eit

\eth

% todo
% 22:30
% $\Cond_{\Z[T]}$ in $M \in \Cond_{\Z[T]}$ is supposed to be the same as \Mod_{\Z[T]}(\Cond^\light_\Z)

% 22:58
So the derived analogs of all these claims hold. Moreover, the derived theory is naive, in the sense that an object in the derived category is derived solid if and only if each homology group is solid in the sense of the abelian category and it is the derived category of the abelian category. 

% 23:15
So that is all the formal stuff. But in the $\Solid_\Z$-theory, it was also important to understand the basic compact projective generator $\prod_\N \Z$, which is always gotten by solidifying the sequence space.

% 23:35
Let me make a claim about that. If we take the $\Z$ power series--- I will just say let's not think of it as a ring; we are thinking of it as a module now. If we take a product of copies of $\Z$, base change it to $\Z[T]$, and then we solidify, we get a product of copies of $\Z[T]$.

\bcl[Solidification of product of copies of $\Z$]
\begin{align*}
((\prod_\N \Z)[T])^{T^\solid} = \prod_\N \Z[T]
\end{align*}
\ecl

\br
% 24:25
A little remark about an interpretation of it. I said $\Solid_\Z$ was analogous to complete non-archimedean topological abelian groups. By definition, non-archimedean means that there is a basis of neighborhoods of the identity, consisting of abelian subgroups.

% 24:45
What would be the analogous interpretation of $\Solid_{\Z[T]}$?
It would be that we have a complete non-archimedean object where, moreover, there is a basis of neighborhoods consisting of $\Z[T]$-submodules. 

\begin{unfinished}{25:00}
% 25:00
For a basic example of a $\Z[T]$-module which is non-archimedean, but does not satisfy that property, you can think of this ring $\Z((T^{-1}))$. 
\er

We cannot find a basis of neighborhoods of zero which are stable under multiplication by $T$, because they are stable under multiplication by $T^{-1}$ instead. 
But in some sense, the theorem is that if we kill just that one object, then we have explained the difference between the two notions.


% 25:35
\ques In the previous theory without $[T]$ it was a little bit different. It was not defined using this "$p$". 

\ans Well, it was basically done this way. Maybe we did not make this explicit and we maybe more talked about just this \todo{add what he points at}, but we definitely talked about this \todo{add what he points at}. But it does make it more clear to think of it that way with the $p$.

\begin{sketch}
All except last property: exactly the same.

\underline{Claim:}
$ M = \RHom_{\Z[T]}$

\end{sketch}

\ques Yes, confused about the $\Z[T]$ itself. Is $\Z[T]$ solid? 


\ans Ah, it is that part of the theorem, because I am claiming in particular that this $\prod_\N \Z[T]$ is $\Z[T]$-solid and $\Z[T]$ is a retract of this. 
So it is something that we need to prove, and we will prove it. 

\ques But $\Z[T]$ is not solid, right?

\ans Oh no, it is. As an abelian group, you mean?

\ques Yes. 

\ans No, every discrete abelian group is solid, because it is generated under colimits by $\Z$. No, it is an important point. Thank you for bringing it up. 

% 26:54
\ques $\Z[[T]]$ (power series) is also solid? 

\ans $\Z[[T]]$ power series is also solid, because it is a limit of objects. You can build it from limits and colimits from $\Z[T]$ \note{not sure $\Z[T]$ or $\Z[[T]]$}. So it is all right.



For the proof, all of these properties, all except the last, those are exactly the same. 
% 27:35
The arguments for all of those things were completely formal, based on the fact that we have this internally projective object $P$ and you are asking that some endomorphism of $\IntHom(P,M)$ become an isomorphism. 

% 27:47
That was all that Peter used when he was proving the analogues of these claims for $\Solid_\Z$.
% 27:55
The fact that we have a good formal theory is already contained in there. 
Then in Peter's lecture, the hard part was identifying the free modules, that when you solidify this sequence space $P$ \note{todo: add what he points at}, we just get a product of copies of $\Z$ ($\prod_\N \Z[T]$). It fills up the whole thing. Thankfully, that part is going to be easier here, because we already have $\Solid_\Z$, and basically thanks to this interpretation of killing some object \note{todo: add what he points at}. 

% 28:35
The key claim is that we can give an explicit formula for the derived solidification. Derived solidification with respect to $T$. This is just an $\IntRHom$ over $\Z$ quotient of this ring by this ring.

\bcl
\begin{align*}
M^{LT}^\solid = \IntRHom_{\Z[T]}(\Z((T^{-1}))/\Z(T), M)
\end{align*}
\ecl

% 29:10
Note that this does have the required map from $M$, because $M$ is $\RHom$. \note{add equation}
\begin{align*}
\IntRHom_{\Z[T]}(\Z(T), M) \iso M
\end{align*}

\ques ...

\ans L derived solidification . The derived functor of this thing here \note{add what he points at, it isn't visible}. I prefer to think of it as the derived analogue of it. 

\ques ... All condensed or all solid?

\ans It is the same. Basically, because of this \note{add what he points at}. I mean the derived...

\ques So when you compute, Grothendieck category, so you have a ... check exactly ... I don't remember ... How do you check underlying are the same? Maybe there is some adjunction... 

% todo, place in right order
% 31:10

% 31:22
\ans Basically, I do not know. Like I said, I tend to think on the derived level from the beginning, but the basic point is that everything has a resolution by these internally projective objects. Then on those, they are the same, and then it is some derived limit of that. 

% 31:40
\ques ... internally projective in $h$ in all condensed, 

\ans In light condensed. But it is really not... that is really not necessary either. This also works in all condensed. I claim it is formal, and I also claim---I do not want to get into the detail right now. Let's maybe discuss after if you still have questions.


% 31:07
\begin{sketch}
The proof of the claim: This follows formally from the fact that this ring $\Z[[T^{-1}]]$ is idempotent over $\Z[T]$, which follows from the very first description we gave of it as the base change of the---we check that the power series ring is idempotent, and we put that at infinity. The idempotency is preserved.

\begin{align*}
\Z((T^{-1})) \tnsr_{\Z[T]}^{L\solid} \Z((T^{-1})) \iso \Z((T^{-1}))
\end{align*}

% 32:50
For example, using the idempotency of that, what is this object here (\note{add label}? 

It is just the homotopy fiber of the inclusion of this into this \note{add what he points at}, and this is the base ring. We can easily check that this idempotency is equivalent to the derived idempotency of this object. 
That means that if we take this expression and we apply it again, we get the same object back. So that is an idempotent operation.
% 33:15
Again, the same idempotence will prove to us that if we take this operation and $\RHom$ from this object $\Z[T]$, we get zero. So this thing is $\Z[T]$-solid. That is not quite everything we need to check, but it is basically everything we need to check. 
It is an idempotent operation, and $M$ is solid if and only if this \note{add what he points at} map is an isomorphism. So it is just completely formal, and I will not write out all of the details.


\end{sketch}

Note: This is a formula for the derived solidification of a general $\Z[T]$-module. 
% 34:04
But something nice happens if your $\Z[T]$-module is base changed from a solid $\Z$-module.

% 34:17
And the next claim is that the pullback functor from $\Solid_\Z$ to $\Solid_\Z[T]$, sending a module $M$ to $M\ tnsr_\Z \Z[T]$ and then you solidify---

\begin{claim}

\begin{align*}
D(\Solid_\Z) \to D(\Solid_{\Z[T]})
\end{align*}

\begin{align*}
M \mapsto (M \tnsr_\Z \Z[T])^{T}^{\solid}
\end{align*}

\end{claim}

This functor is $T$-exact, preserves limits and colimits, and it sends $\Z$ to $\Z[T]$.

% 35:11
If we prove this, then in particular we get this claim \note{todo: add label}, because this is that functor applied to $\prod_\N \Z$. 
% 35:22
I claim that the functor commutes with products, so then it is enough to understand what happens with $\Z$. But I already also claimed that $\Z$ goes to $\Z[T]$, so we would be done.

\begin{sketch}
We just take this formula for the solidification, and we plug in the case where $M$ is also induced. We get that $(M \tnsr \Z[T])^{LT^\solid}$ is the same thing as $m [T]$ (by that I mean $m[T]/\Z[T]$) \note{todo}. 
\begin{align*}
(M \tnsr \Z[T])^{LT^\solid} = \IntRHom_{\Z[T]} ( , M[T])
\end{align*}

% 36:23
Now we are computing an $\IntRHom$ over $\Z[T]$. The way to do that is to disambiguate the two occurrences of $T$ and then equalize them at the end. So that is calculated as $\Z[[U]]$, and a two-term complex (I will write it vertically so to speak). 
\begin{align*}
= (\IntRHom_\Z ( U \Z[[U]] [-1], M[T]) )
\end{align*}

Then you equalize $T$ and the shift operator on here, which is induced by multiplication by $T$
% todo

\note{todo}

\end{sketch}


% 
Multiplication by $T$ is here given by sending $U$ to zero, $U^2$ to $U$, $U^3$ to $U^2$, etc. 
So if you do that there, then that induces an endomorphism here, and that is the multiplication by $T$ on this resulting thing.

% 39:25
\ques Sorry, I missed what you said. What is the isomorphism to this? 


\ans This bit of somewhat silly notation is the homotopy fiber of this map, or some shift of a mapping cone. That is the same as this. It is also the same as this.

% 39:50
Now we are done. Now this functor again---this is internally projective in $\Solid_\Z$, so this functor is $t$-exact and it preserves limits and colimits in $\Solid_\Z$. But limits and colimits in $\Solid_{\Z[T]}$ are calculated on the underlying level, because it is just part of a module category.

% 40:12
The last claim is that $\Z$ goes to just the usual polynomial ring in one variable. 
What happens when we plug $\Z$ in $\RHom(...)$? we are taking $\RHom$s from this product of copies of $\Z$ to $\Z$. It turns into a direct sum of copies of $\Z$. We can check that what we get is just the usual $\Z[T]$, even with the $\Z[T]$-module structure. There is a natural comparison map which we see to be an isomorphism.

% 40:45
\ques Someone proved (it is not related directly to the theory) $R\Hom$ from the infinite product of copies of $\Z$ to $\Z$ is a direct sum. But this is not---here you are doing it in---

% 41:03
\ans this is easier.

% 41:15
We can also do other examples of such a thing. Let's do another example. 
That finishes the proof of the theorem, so we now have a grip on this $\Solid_{\Z[T]}$ theory. 
I want to advance the interpretation that the $\Solid_{\Z[T]}$ theory is like working over $\Z[T]$ without the solidification, is like the affine line. $\Z[T]$ with the solidification is like the closed unit disk. That was the interpretation that I started with.

\bex
% 41:45
Let's do an example again in a non-archimedean field. Let's base change to a non-archimedean field and see what happens.
Let's look at $\Q_p[T]$. That is functions on the affine line. 
Now we want to restrict to the closed unit disk, in the sense that we have just described. 

So we take the $T$-solidification $(\Q_p[T])^{T^\solid}$ of this.

% 42:12
This is an instance of this functor here \note{todo}, and I just said this functor has all the properties in the world. It commutes with colimits, so I can again take the $\frac{1}{p}$ to the outside. It also commutes with limits, so I can take the limit over $n$ of $\Z/p^n\Z[T]$, $T$-solidified, and then $\frac{1}{p}$ at the end.

\begin{align*}
(\Q_p[T])^{T^\solid} = (\Z_p[T]^{T^\solid})[\frac{1}{p}] = \lim_n .. [\frac{1}{p}]
\end{align*}

% 42:49
Now we are applying this functor there to $\Z_p^n$, but that is just two copies of $\Z$. So we get the same answer as for $\Z$, it is just discrete. This is just inverse limit over $n$ of $\Z_p^n\Z[T]$, $\frac{1}{p}$.

\begin{align*}
= (\lim_n \Z / p \Z [T])[\frac{1}{p}]
\end{align*}

% 43:06
In other words, it is $\Z[T]$, $p$-completed, $\frac{1}{p}$. These are the functions on the closed unit disk, as desired.

\begin{align*}
= \Z[T]_{\hat{p}}[\frac{1}{p}]
\end{align*}

\eex

% 43:36
Maybe I forgot to give an interpretation of the coefficients here.
So again, this is some power series with coefficients in $Q_p$, but here (\Z[T]_{\hat{p}} [\frac{1}{p}]) the coefficients tend to 0, $p$-adically. 
% 44:00
So it is a smaller ring of functions, that converges on a bigger region. Then we are allowed to be .. \note{todo} on the boundary, so to speak. 

% 44:15
Let's play a fun game. I have been talking about the open unit disk and the closed unit disk, or the closed unit disk and the complement of the closed unit disk. 
% 44:30
But there is always this funny question of what is exactly open and what is exactly closed. Because in rigid geometry, the closed unit disk is considered as open. The open unit disk, I suppose it is also open in a sense, but it is not quasi-compact.
% 44:50
So let's play a game. 

% 44:54
\ques ... The Berkovich viewpoint, which is pretended somehow .. another way .. then it is closed 

% 45:04
\ans I claim that our formalism is \todo{"or is not?" can't hear this well} going to give a definite answer to this question, of what is open and what is closed.

% 45:15
\subsection{What is closed and what is open}
Let's take a look at the categories we have been attaching to the various geometric objects. For the affine line, ... the polynomial ring in one variable, but then the linear algebra category is just $\Z[T]$-modules in solid abelian groups,$ \Mod_{\Z[T]}(\Solid_\Z)$. 
% 45:42
Let me go to the derived perspective.

\begin{align*}
D(\Mod_{\Z[T]}(\Solid_\Z))
\end{align*}

But then, inside there we had this derived solid. So we had 

\begin{align*}
D(\Solid_{\Z[T]})
\end{align*}

But it is better to think of that as a quotient, because the symmetric monoidal functor is the derived solidification functo, which goes like this.

\begin{align*}
D(\Mod_{\Z[T]}(\Solid_\Z)) \xrightarrow{ ()^{LT^\solid} } D(\Solid_{\Z[T]})
\end{align*}

% 46:18
But then on the other hand, we had the complement. The complement was the open unit disk, or rather, the open unit disk moved to infinity. \note{add what is pointed at}
So that is this thing, and that corresponds to $D(\Mod_{\Z((t))} (\Solid) )$.
% 
I think someone asked before, but I forgot that you had the solidification, which is $\Z[T]$. There should be another on the other side.
% todo

% 46:52
It is a forgetful functor. 
A priori, we are forgetting the extra module structure, but it is in fact a fully faithful inclusion because of the idempotency. The idempotency of this thing means that there is at most one $\Z[T]$-module structure on any $\Z((T^{-1}))$-module structure on a $\Z[T]$-module.

% 
This is like a localization sequence, or what have you. 
This is the symmetric monoidal quotient of this by this thing, which is an ideal in there. Wat adjoints do we have, and which of them are well-behaved, and so on?

% 47:25
We know that this one \note{todo} has a right adjoint, which is given by the inclusion. But it also has a left adjoint. The left adjoint is given by sending $M$ to the fiber. So you take $M$, and then you base change it to infinity, and you take that cone diagram here. So it is the fiber of $M$ mapping to $M[T]$ over $\Z[T]$ with the $\Z((T^{-1}))$.

% 48:20
This left adjoint is, in a sense, better behaved than the right adjoint, the naive inclusion. By the measurement that it is better behaved, they both commute with colimits, but this one satisfies a projection formula with respect to this fundamental functor, which is the symmetric monoidal functor. So this left adjoint is linear over these two symmetric monoidal categories. So this is better.

I am going to call this one $j^\ast$, and this one $j_!$, and this one $j_\ast$, this inclusion here. 

% 48:57
On the other hand, for here, we have this functor here, which I will call $i_\ast$, the inclusion there. 
So it has a left adjoint, also $i^\ast$, which is just the base change functor, which is symmetric monoidal. 
So that is the more fundamental one from this perspective. 
But it also has a right adjoint $i^!$, which is given by some $\RHom$, but it is less well-behaved again, because here, this one satisfies a projection formula with respect to this $i^\ast$. So this functor is linear over this symmetric monoidal category, just like this functor is linear over this symmetric monoidal category.

$\ast$ is an adjoint, the inclusion has $!$ on it.


\ans It does, but that one we do not talk about.

\ques Yes, now this was the question I...


\ans Yes. Because already this one is not as nice as, I mean, the good adjoint is up here.

% 50:00
The interpretation that this suggests---this is exactly like if you have a topological space $X$, and then you have $Z$ a closed subset, and then $U$ the complement open, then you get, if you have derived categories of sheaves, you have exactly the same thing. 

\begin{align*}
D(\Sh(\Z, \Z) \xrightarrow{i_\ast} D(\Sh(X, \Z) \xrightarrow{j^\ast} D(\Sh(U, \Z)
\end{align*}

Where you have the open over here, you have this thing satisfies a projection formula, you happen to have this other guy, but it is not as well-behaved. 
Then you have $i_\ast$ from $\mathcal{D}(Z)$, $i^\ast$, and then $i^!$. 
So formally speaking, it behaves exactly the same way. They are both special cases of the same thing, which is: you have a symmetric monoidal category, and you have an idempotent algebra in it, and it generates the whole situation.

% 51:09
In this case, you have this category $\Solid_\Z$ and this idempotent algebra $\Z[[T^{-1}]]$. Here you have this category $\Sh(X,\Z)$, and then the pushforward of the constant sheaf from the closed subset. These are completely analogous formalisms.

% 51:27
That tells us that from our perspective, the closed unit disks should definitely be thought of as open. 
The open unit disks should definitely be thought of as closed. 

As far as I am concerned, the question is settled. 

% 51:47
Another funny thing is that if think about this perspective, it also tells you that Zariski opens in usual algebraic geometry should be thought of as closed. I think that is also true. I can explain why, if people want to know. 

% 52:15
\ques You start from the usual theory, like patching a sheaf, 
usual topology, like Zariski or étale or analytic. But then you somehow ..
decide that now, the Zariski opens are closed

% 52:44
\ans I would like to convince you of that, but not right now. 

% todo

You said that the rational opens are closed.

% 53:12
For example a distinguished open is just the ring with the function inverted - that is idempotent.

% 53:21
\ans Maybe I should say quasicompact opens are closed. Thats probably...

%
\ques The derived direct image of the structure sheaf is idempotent?

\ans Yes.

% 53:41
By the way, we are going to be guided by this sort of thing in setting up the definition of analytic stack and so on. One of the things we discovered is that when we move to this condensed/solid/whatever context, we get six functor formalisms in large generality on derived categories of "quasi-coherent sheaves". They have really nice interpretations.

% 54:08
For example, if we think of this object \note{add what is being pointed at} as being what sits at infinity, then it makes sense that this is extension by zero. We are taking our sections and killing the ones that live near the boundary. 
It all plays quite nicely, and we showed how to give proofs of things like Serre duality and so on using these formalisms.

% 54:25
So we take this perspective seriously - the derived categories and functors between them are going to dictate to us what the geometry looks like.

% 54:36
There was another thing I remembered in the break that I forgot to mention. 
I said that this \note{add what is being pointed at} is $t$-exact and preserves limits and colimits, but I want to caution you that this $t$-solidification from $\Solid_{\Z[T]}$ is not $t$-exact. It is only off by one. I said it was given by $\RHom$ from this object which has a 2-term compact resolution. So it is only off by one from being $t$-exact.

% 55:05
Everything connective goes to something connective, and everything anti-connective goes to at most a 1-shift of something anti-connective. So $t$-solidification is not exactly exact, but it is very controlled. 

\ques The composed map is?

\ans Yes. Exactly.

The first map in the composition is $t$-exact, but the second is not. 
% todo


\ans Yes. It is a bit funny.


% 55:30
So now I want to motivate what I am going to do in the rest of this lecture.
What is one place where we are probably going to go? 
\begin{vista} % motivation, todo macro
We want to look at solid rings, i.e. commutative algebra objects in this tensor category $\Solid_{\Z}$.

$R \in \CAlg(\Solid_{\Z}, \tnsr)$ % todo macro
\end{vista}

Generalizing our discussion when $R$ is $\Z[T]$, we want to see subsets like closed/open unit disks. Of course, when you have a big algebra, you will get many more such subsets.

% 56:23
We want to organize what you see in a nice way. 
% 56:30
It is not necessarily the most general thing, but we are going to take the things you see over $\Z[T]$ and base change them along all possible maps $\Z[T] \to R$, i.e. all possible functions in $R$.

% 56:45
What we will end up with is the statement that the derived category of $\Solid_\Z$-modules localizes along the valuative spectrum of the underlying discrete commutative ring of this solid ring. 

$D(\Mod_R (\Solid_{\Z}))$ localizes along $\Spv(R(\ast})$

You take the valuative spectrum of $R$ viewed as a discrete ring.

%
The valuative spectrum is a residue field and valuation. 
The valuation does not have to be integral on $R$, just any valuation. It has a topology which induces the topology on things like $\Spa$, but it is more general - sometimes one gets points which are not in $\Spa$.

%
This space is similar to the usual spectrum of a commutative ring. 
It is a spectral space with a basis of quasi-compact opens, and even a particularly nice basis analogous to distinguished affine opens in algebraic geometry.
% 58:44
While a distinguished open in algebraic geometry is parameterized by a single element of the ring, here you have to take a bit more data.

%
The basic opens are the so-called rational opens. 
You take finitely many functions $f_1, \ldots,f_n, g$ and form $\{x \mid |f_i(x)| \le |g(x)|\text { for all i}\}$. %todo

The interpretation is that you invert $g$, so we are inside the Zariski distinguished open for $g$, but then we shrink further by requiring that $|f_i| \le |g|$, $\forall i$.
Also $g \neq 0$  and %todo 

% 59:45
I am saying that this localizes on this meaning. 
You have a sheaf of categories, a sheaf of symmetric monoidal categories. 
% 59:53
What we are going to attach to this thing $X(...)$ is a version of the solid theory. 
% 1:00:00
So we look at those $M$ in $D(\Mod_R(\Solid_{\Z}))$ such that, 

\ben
	\item 
	\item $\IntHom(P,M) \xrightarrow \IntHom(P,M)$
\een
first you want to say that multiplication by $g$ on $M$ is an isomorphism. 
$g \cl M \iso M$
Second, you want to say that if you take $\IntHom(P, M)$, and you take $f_i^\ast \tnsr [-1]$, this should be an isomorphism for all $i$. 

$\IntHom(P,M) \to  \IntHom(P,M)$ % todo
% 1:00:30
Or in other words, $\frac{f_i}{g}$. Sorry, so in other words, <e think of all these objects as maps from say $\Spec(R)$, so to speak, to the affine line.
% 1:00:58
Then we want that $g$ lands in the standard Zariski nonzero locus. We want that the $\frac{f_i}{g}$ land inside the closed unit disk.

$ g \cl \Spec(R) \to M $

$ \frac{f_i}{g} \cl \Spec(R) \to $ % todo triangles

% 1:01:40
So I am not going to go into details about that.


\ques Here, this $|P|_B$ just refers to the locale correspond to... 

% 1:02:05
\ans We can think of it as a locale, but it is also a topological space.

% 1:02:13
The points have a nice description and so on.

% 1:02:23
In particular, I am claiming this category $D(\Mod_R (\Solid_{\Z}))$ localizes along the space $\Spv(R(\ast))$.
% 1:02:27
In particular, we get a structure sheaf on the space $\Spv(R(\ast))$. The goal for the rest of lecture, is make this structure sheaf explicit and probably start to compare to Huber's theory. 
% 1:03:22
We are eventually going to produce this data by very easy formal means, but it requires some language and setup. 
So we can not do it yet, but I want to make at least this part of it explicit already at the beginning.

\subsection{Making structure sheaf explicit}

% 1:03:19
\begin{goal}

Goal for the rest of the lecture
\bit
	\item Make $O(X())$ explicit
	\item Compare to Huber's theory
\eit

\end{goal}


$R \in CAlg(\Solid_{\Z}), \tnsr), f \in R (\ast)$
$\leadsto \Z[T] $


% 1:03:58
Let's start with this generality. We have a solid ring, and let's take an element $f \in R$, or I should say in the underlying discrete ring of $R$ in case there is ambiguity. 
That in particular gives you a map from $\Z[T]$ to $R$ which sends $T$ to $f$. 
Then we can see how these loci that we have identified can correspond to properties of $R$.

$R \in CAlg(\Solid_\Z, \tnsr)$
$f \in R$
$\Z[T] \xrightarrow{T \mapsto f} R$ \label{map:07-t-f}

% 1:04:47
Let me make a definition. 
\bd[Topologically nilpotent]
$f$ is \textit{topologically nilpotent} if this homomorphism factors through the power series ring $\Z[[T]] = P^\solid$.
\ed

I want to say $f$ is power bounded if this map \ref{map:04-t-f} geometrically factors through the closed unit disk. 
$R$ is an algebra over $\Z[T]$ In particular, it is a module over $\Z[T]$, and we can ask that it be solid.
\bd[Power bounded]
$f$ is \textit{power bounded} if $R \in \Solid_{\Z[T]} \iff \IntHom(P, R) \to \IntHom(P, R)$ % todo
\ed

%
\ques In the first definition, the factorization is unique. 

% 1:05:40
\ans Yes, it is unique if it exists because of this idempotency. So that is an interesting fact. $\Z[[T]]$ is also the free module on a nullsequence. 
% 1:05:50
Even though there are Hausdorff solid abelian groups that have non-Hausdorff behavior, this limit is still unique if it exists.

% 1:06:10
So in the definition of solid, we are imposing that certain limits exist uniquely, even though we have non-Hausdorff behaviour. That is the same thing as saying that if we take $\IntHom(P, R)$, $f^{\ast} \tnsr [-1]$, that this is an isomorphism. That is just... Oh, someone is talking.

%
Yes, the first condition is saying that it is a nullsequence which is the $p^n$ go to zero. 

I was going to explain the relation with classical definitions, but it is quite immediate for this one that it is the same as the classical definition. So I will just repeat what Peter said. 

% 1:07:00
So this is

% 1:07:15
Saying that that factors through $P$ is the same thing as saying that this sequence $1, f , f^2, \ldots$ extends to a nullsequence, which is exactly the same thing as being topologically nilpotent. 

% 1:07:30
The new phenomenon in the solid case is that such a limit is unique if it exists. 

%

\gabber it factors through in the sense of rings, so in the sense... of the uniqueness \note{not sure}.  ... You can ask factorization is.

\answer Yes, in the sense of $\Z[T]$-modules is enough, or I am not sure. 
I do not think so. Thank you for the comment. Yes, just as rings.

% 1:08:25
Here is a lemma giving basic properties. We write $R^\circ$ (this is again standard notation in Huber's theory) for the set of power-bounded elements, and $R^{\circ \circ}$ for the set of topologically nilpotent elements, which is a subset of $R$. I am going to prove that.

\bl
$R^\circ \subs R()$ is integrally closed subring
$R^{\circ \circ} \subs R^\circ$ is a radical ideal.
\el

%
The lemma is that $R^\circ$, inside this ring here, is an integrally closed subring, and $R^{\circ \circ}$, first of all, is contained in $R^\circ$, and it is a radical ideal.

%
I am still confused about this nullsequence. 
Something is an nullsequence in the sense that the map extends to another sequence, then you do not know that it factors as a ring map, that this is a ring map? 
That seems correct to me, yes. 
Okay, so it does not mean topologically nilpotent in your definition, in your sense. 
But on the other hand, if you have something that comes from a Hausdorff topological ring, for example, if the target is quasi-separated, then independently of asking about the algebra structures, the limit is unique if it exists. 
Yeah, quasi-separated was this analog of Hausdorff in the condensed setting.

%
Still, if you start with something Hausdorff, then it is automatically a ring map if it is an nullsequence, yes, because of density. 
So we are not sure whether "topologically nilpotent" in the nullsequence sense is always the same as this other notion. 
Okay, Hausdorff, yeah.

%
So, proof. Why is this a subring? The first thing to check is that it has a unit (that is part of what I mean by "subring"). But if you look at the definition of "solid", that is one way of saying it. Put $f=1$ here, okay.

\begin{sketch}
If $1 \in R^\circ$: definition of solid.
$f,g \in R^\circ \leadsto$ if $F \in \Z(x,y)$ then $F(f,g) \in R^\circ$

$\Z[x,y] \xrightarrow R$



Resolve $R$ by $\bigoplus \prod_\N \Z[x,y]$'s.

Solidify $X^\solid$: $\bigoplus (\prod_\N \Z[x])[Y]$
Solidify $Y^\solid$: $\bigoplus (\prod_\N \Z[x,y])$
\end{sketch}

%
Now, I want to show that if $f$ and $g$ are in there, I will prove that it is a subring by showing that if you apply any polynomial to $f$ and $g$, then you are still in there. So how can we do this? 
Maybe there are different ways, but I think the cleverest one is: we can look at the map from the polynomial ring in two generators to $R$ which sends $X$ to $f$ and $Y$ to $g$. Our hypothesis is that $R$ is a $\Solid_{\Z[X]}$-module \note{todo, check} and a solid $\Z[Y]$-module, and what we want to conclude is that for any map from the polynomial ring in one generator $T$, $R$ is a solid $\Z[T]$-module.

% 1:12:45
So first, resolve $R$ by its resolution. 
It is definitely solid as an abelian group, so we can resolve $R$ by direct sums of compact projective generators. 
But we know that $R$ is a solid $\Z[X,Y]$-module. So if we solidify with respect to $X$, then what does this turn into? 
It turns into a direct sum of products of copies of $\Z[X,Y]$ by the properties of derived solidification that I proved earlier. 
% 1:13:48
But then it does not change $R$, so that is still a resolution of $R$. 
Then we solidify with respect to $Y$ and we get a direct sum of products of copies of $\Z[X,Y]$ again by the same reasoning. 

\begin{align*} 
\text{solidify $Y^\solid$:} \bigoplus(\prod \Z [X,Y])
\end{align*}

% 1:14:05
In total, we see that $R$ can be resolved by these $\bigoplus(\prod \Z [X,Y])$, but each of these is solid over $\Z[T]$, because it is a colimit of limits of things which are solid over $\Z[T]$. 

% 1:14:26
% todo

This is solid over $\Z[T]$ because it is discrete. Then this is a product and that is a direct sum, so in total it is solid over $\Z[T]$.

% 1:14:46
So I did not use directly the definition of solid. Instead, I used the description of these generators 

% 1:15:17
The topologically nilpotent elements are all power bounded. You can use a very similar argument. What we have is a map
\begin{align*} 
\Z[[X]] \to R
\end{align*}
% todo label arrow
which sends $x$ to $f$.
Which means that $R$ becomes a $\Z$-power series $X$-module. 
% 1:15:35
So now if we resolve $R$ by direct sums of products of copies of $Z$
M todo

% 
We conclude that $R$ is solid over $Z[X]$. Oh, I switched to $T$ somehow. Switch to $X$ somehow.

% 1:16:15
We conclude that this composition $R$ is solid as a $Z[T]$-module. 



Oh, I did not prove... I forgot to prove it is integrally closed.

\bq[$R^\circ$ is integrally closed]
\begin{align*} 
f^n + c_{n-1}f^{n-1} + \ldots + c_0 = 0
\end{align*}
\eq

% 
Let's do that. It is again a very similar argument. 
Let's say that we have an equation of the form $\sum c_i t^i = 0$ where all $c_i$ are power bounded. Again, we just make the universal thing. We have $Z[x_0, \dots, x_{n-1}]$ and over that we have the ring where you adjoin another variable, let's call it $t$, and then you set the equation $\sum x_i t^i = 0$.

% 1:17:30
We have our solid ring $R$. By hypothesis we have a map here, such that when we compose to here, $R$ becomes a solid module over each of the $x_i$ variables. What we want to show is that when you compose here, it becomes a solid module over this $t$ variable.

% 1:17:50
I will just say it quickly in words. You use the same trick. 
You resolve $R$ first as just a $Z[x_0, \dots, x_{n-1}]$-module in solid abelian groups and you solidify with respect to each of the variables. 
You find yourself built out of products of copies of this ring $Z[x_0, \dots, x_{n-1}]$, but you are also a module over this \note{todo}, so you can tensor up to this. 
% 1:18:13
But that is a finite free module over that ring, so then that will just go inside the products and you find that you are resolved by a direct sum of products of copies of these objects. Because this individually is solid, and solid is closed under limits and colimits, you deduce that that is solid as well. 
% 1:18:28
The key here is that this is finite as a module over this, so that tensoring with it you can bring inside the product. 
Finite free, yes. Although, that is not really necessary. 
The ring is Noetherian and you can resolve... Finite would be enough in fact.

QA
Finite is all 
It is a Noetherian ring.

% 1:19:06
I will leave the rest to you. They are completely analogous arguments, like why it is an ideal and why it is a radical ideal. These are fun exercises in that style of argument.

% 1:19:46
So now we can describe this structure sheaf. 
Suppose we have again a solid ring $R$ and then $g, f_1, \dots, f_n \in R^\solid$. 

\bcl
$\exists$ initial solid ring $R \frac{f_1,\ldots,f_n}{g}^\solid$ such that 
\ben
	\item $g$ is invertible in $R$
	\item $\frac{f_i}{g}$ is power bounded in $R \frac{f_1,\ldots,f_n}{g} \forall i$
\en
\ecl

% 1:20:18
That encodes the idea I was talking about, where you want $f_i/g$ to go to the closed unit disk, you want $g^{-1}$ to be invertible.


The proof is that you can just construct the object. 

\bq
$R[x_1,\dots, x_n]^{x_1^\solid, x_2^\solid, \ldots, x_n^\solid}
\eq

% 1:21:42
You could first invert $g$, but maybe I will invert $g$ at the end. What you can do is take $R$ and adjoin polynomial variables $x_1, \ldots, x_n$ and then solidify with respect to all of them, which recall does something when $R$ is not discrete.
% 1:22:03
Remember we had the example of one variable and $R$ was $\Q_p$, then this gave us the Tate algebra.

% 1:22:10
Then we can say that these variables are supposed to be $\frac{f_i}{g}$, so maybe $gx_i = f_i$ for all $i$. todo

It does not matter at which point you invert $g$, but let's do it at the end. This obviously satisfies the correct universal property.
% 1:22:50
First we freely adjoin solid variables, then we impose the relations which guarantee that kind of thing. There is one small thing to check, which is that after you take this in which these are definitely solid and then you do these operations, you need to see it is still solid. But that is because it is all colimits.

% 1:23:15
So this is the thing you are looking at. 
Now I want to make a caution here.

\begin{warning}
This is not exactly the value of the structure sheaf on $X(\frac{f_1, \ldots, f_n}{g})$. It is $\Pi_0$ (value of structure sheaf).

% 1:24:02
So I said, so remember when I was discussing this six-functor formalism and this localizing the category, I was always .. to say the derived category. 
% 1:24:12
In fact, these things (like the six functor formalism and so on) really only work at the derived level. 

% 1:24:20
That means that what you are producing, a priori is a derived sheaf, not an ordinary sheaf.
% 1:24:26
So that is okay, but in almost all practical cases, all $\pi_i = 0$ for $i > 0$. So in practice, it does not seem to cause trouble, but it is an important thing to keep in mind.
\end{warning}

%QA
Derived structure sheaf


% 1:24:52
The second warning.
\begin{warning}
Even if $R$ is a very nice Huber ring, then this quotient may not be quasi-separated. So it is an analogue of restricted power series in general. 
Yes, exactly. This will always be the usual Tate algebra in many generators. That follows from the arguments we gave for any sort of Huber ring $R$. But then when you take this quotient by the ideal generated by these elements, it might not be closed. So in principle you could be having a non-Hausdorff quotient here.
\end{warning}

\ques When you say "Huber ring", do you mean complete Huber rings?

\ans Yes, I mean complete Huber ring, thank you. But this theory is defined for complete Huber rings as well, or no? This theory is only defined for complete Huber rings, the theory I am discussing, because you cannot define the condensed set of any topological thing. You can define it, but it will not be solid unless the thing is complete in general. 

Okay, you want it solid. So you want complete, and then the other guys, when you do this, it is still solid but does not come from---it is an analogue of, yes.

Yes, but again, in almost all practical cases, or maybe all practical cases, it is quasi-separated, i.e., Hausdorff.

\subsection{Relation to Huber's theory}

% 1:27:25
What I was trying to aim for is the relation to Huber's theory.  I guess I probably will not get there. 
% 1:27:38
We will definitely discuss this in more detail later, but just as a preview: if $R$ is a Huber ring (and do not worry if you do not know what that is, it is just a certain nice topological ring that people use in non-archimedean analysis), and let's say complete---no, just Huber, I am just doing the rings now, not the pairs. 
% 1:27:59
If the ideal generated by $f_1, \ldots, f_n$ inside $R$ is open, which is the condition that describes rational opens in the space of continuous valuations as opposed to the space of arbitrary valuations, then in this case Huber defines the ring of functions here. 

% todo macros Huber, 

$R < \frac{f_1, \ldots, f_n}{g}> ^\huber $

You could ask what the relationship is with this thing that comes from the solid theory.

% 1:28:35
This is the "quasi-separification" of this more generally defined thing that we have here. 

%todo
\pi_0 $ R < \frac{f_1, \ldots, \f_n}{g} > ^ \text{solid} $ 

So the solid thing, or for emphasis I should put the $\pi_i = 0$ as well---you can get the Huber thing functorially from this more general thing, in particular from our structure sheaf. 
But they are not necessarily equal in general. But in all practical cases, they are equal. That is the general outline of the story.

% 1:29:23
In general, if you always use the derived structure sheaf, it always satisfies the---

yes, 


So it is always a sheaf. 


Yes. So that is an important point to mention. 


There was this little fly in the ointment in Huber's theory, that in the general setting, he defined a structure sheaf, except it was not a sheaf. It was only a presheaf.
% 1:29:49
In all practical cases it was a sheaf, but still, the general theory was missing something for that reason. 
That is fixed by this. If you no longer care about things being quasi-separated and non-derived, then you get a good plain old structure sheaf. 
You get even more, you get the derived category of quasi-coherent sheaves which localizes. 
Also, you get the possibility of defining all of these things even without this condition being present.

\subsection{Questions}

\ques Do we have any use of discontinuous valuations? 

\ans Any use? Yes, I know Pierre Colmez likes them, so he has this notion of adux spaces. Those correspond to certain---

% https://en.wikipedia.org/wiki/Pierre_Colmez

\ques Yes, but you do not really use them.

\ans I think he used them, but you did mean me? Look, I just build the theory, I do not use it. He calls them "ad hoc spaces", I do not think he developed the theory, he just did it in an example. So he wanted to exactly include things like this open unit disk.

% 1:31:10
So this is objects like the $\Z_p[[T]][\frac{1}{z}]$. I think in his paper where he does this $GL_2$ mod $p$ Langlands. This object is a prototypical example of a topological ring that is not a Huber ring. So in the usual theories of analytic geometry, the open unit disk, you can not think of it as an affine space, because the ring the ring that it corresponds to is not one of your allowed rings. So you always have to think of the open unit disk as a union of the closed unit disks contained in it.

% 1:31:45
This non-quasicompact object, to fit it inside Huber's theory---but Colmez advocates that sometimes it is better to just work with this thing as if it were affine. That is something that our theory easily accommodates.

% 1:32:00
\gabber There are people who did---I forgot the name of this---and constructed some notion of rigid, I forgot the name. Someone wrote some paper on this, on like a variant of... I forgot the name of the... There was some paper some time ago, but I am not sure.

% 1:32:38
\ques So every solid $\Z$-algebra...

% 1:32:42
\ans So this notion of analytic ring, we have not defined it yet. It organizes a lot of discussion. But indeed, if you have an algebra in solid $\Z$-modules, you get what we call an induced analytic ring structure. I said, the module category you have, and that sits inside condensed $\R$-modules \note{not sure if $R$ or $\R$}, and that is an analytic ring. 

\ques Yes, but in the case of $\Z$, we had something more natural \note{could not hear the rest of the remark}.

% 1:33:12
\ans More natural, it is arguable, it is more complete. There are two things. They exist. They do, you want them both, I think, 

% 1:33:22
\ques And can be extended to any discrete ring, right?

% 1:33:27
\ans Yes, that is right. 

% 1:33:30
\ques Not in general? Not general solid $\Z$-algebra? 

% 1:33:33
\ans For a general solid $\Z$-algebra, I do not necessarily know of any completely canonical... One thing you could do, is you could take all of the power-bounded elements and force all of them to be solid. That would be the maximally complete thing that you can get via the stuff that we have developed. But there could be further completions of that, as far as I know.

% 1:34:00
\ques With the definition of solid analytic rings we used in the very first lecture, if we use this instead of a regular ring here, do we get something similar to like Huber's theory for Huber pairs, instead of just regular rings? 

% 1:34:20
\ans Could you repeat the question? I am not sure I understood.

% 1:34:25
\ques In the very first lecture, we defined solid analytic rings. With the ring and the derived category. If we try applying something similar here to analytic rings...

% 1:34:37
\ans Do you mean this discussion of this object here \note{add what he points at}? You can do that. 

\ques But do we recover, like...? 

% 1:34:47
\ans Yes, you recover the theory with the $R^+$ in there as well. 

\gabber So elements $R^+$, you require solidity. 

\clausen Exactly.

\gabber Not for all of the guys.

\clausen Exactly. We will discuss this. You can add that extra flexibility into the picture, yes.

% 1:35:03
\gabber The fact that $R_{00}$ \note{todo}, this is automatic, that those are the topologically nilpotent ones.  

% 1:35:12
\clausen They are automatically solid, exactly.


\clausen Yes, so it actually fits remarkably well with Huber's theory.

% 1:35:25
\ques Like, you take tensor products of analytic rings, you have to first solidify one another, have to take... %todo

% 1:35:33
\clausen Yes, usually. 

\ques So here it is still the same. 

\clausen Here you do not have... These \note{add what he points at} solidifications all commute with each other, so there you do not need to... Yes, they are all given by $\RHom$'ing out of something, and any two $\RHom$'ing out of commutes with each other.


\end{unfinished}