% !TeX root = ../AnalyticStacks.tex

\section{\ufs Localization of solid analytic rings (Clausen)}

\url{https://www.youtube.com/watch?v=lJTLj8gYAtg&list=PLx5f8IelFRgGmu6gmL-Kf_Rl_6Mm7juZO}
\renewcommand{\yt}[2]{\href{https://www.youtube.com/watch?v=lJTLj8gYAtg&list=PLx5f8IelFRgGmu6gmL-Kf_Rl_6Mm7juZO&t=#1}{#2}}
\vspace{1em}

\begin{unfinished}{0:00}

Today, we will explore localization in the setting of solid analytic rings. But first, I want to start with a recap of last time.

Last time, we defined the notion of an analytic ring, which is a pair $R = (R^\tri, M)$ where $R$ represents the entire structure, comprising: 
\begin{itemize}
\item $R^\tri$, a commutative condensed ring.
\item $\Mod_R$, the category of $R$-modules. $\Mod_R$ is full subcategory of $\Mod_{R^\tri}$, satisfying certain closure axioms. 
\end{itemize}

Key properties of $\Mod_R$  include:
% 2:00
\bit
\item Closure under limits, colimits, and extensions: The first one is that $\Mod_R$ is closed under all limits, colimits, and extensions. In particular, it should be an abelian subcategory. But even more, because I am allowing infinite colimits and limits here.
\item Internal Ext condition: If $M \in \Mod_{R^\tri}$ and $N \in Mod_R$, then $\IntExt^i_{R^\tri}(M,N) \in \Mod_R, \forall i$. This condition was necessary to get a good tensor product on this category and on the derived analog as well. I will make a couple more remarks in a second.
\item Containment of the unit: $R^\tri$ itself, the unit in this category $\Mod_{R^\tri}$, must belong to $\Mod_R$, meaning 
\begin{align*}
R^\tri \in \Mod_R.
\end{align*}

\eit

%and $N$ in $\operatorname{Mod} R$, the internal $\operatorname{Hom}$ from $M$ to $N$ also lies in $\triangle R$. Finally, the unit $R$ itself lies in $\triangle R$.

% 3:15
There were a few technical points that came up last time that I would like to address now. One question concerned whether the existence of left adjoints is automatic. There was a claim that it was, justified by a theorem of Adamek and Rosický, known as the \emph{reflection principle} \note{add ref, Locally Presentable and Accessible Categories 1994?}. 

This theorem states that if $C$ is a presentable category and $D$ is a full subcategory of $C$ that is closed under all limits, and if there exists a regular cardinal $\kappa$ such that $D$ is also closed under all $\kappa$-filtered colimits, then $D$ itself is presentable, and the inclusion functor $D \to C$ admits a left adjoint.
% todo
% 4:50
If $\kappa$ is the first infinite cardinal $\aleph_0$, if not then this is the $\kappa$-filtered colimit % todo. 
you make $\kappa$ bigger it is a weaker condition that it be closed under $\kappa$-filtered colimits. Then $D$ is presentable and ... \note{todo}.

\ques What about the derived category?

\ans The $\infty$-category version is proved by Regonov-Schlank. \note{todo, add ref}
% The ∞-Categorical Reflection Theorem and Applications
% https://arxiv.org/abs/2207.09244

% 6:10
\begin{remark}
I would like to address another technical point that came up in the previous talk. Recall that we were discussing the derived analog of this definition, where we defined the derived category of $R$, denoted $D(R)$ as a full subcategory of the derived category of $\Mod_{R^\tri}$. This subcategory consists of those objects $M$ for which all homology groups $H_i(M)$, belong to the abelian category $\Mod_R$ (for all $i$ $R^\tri$).
\begin{align*}
D(R) \sube D(\Mod_{R^\tri}) (= D(R^\tri))
\end{align*}
\begin{align*}
D(R) = { M \in \Mod_{R^\tri} \mid H_i(M) \in \Mod_R \text{ for all } i }.
\end{align*}
\end{remark}
\note{todo, check this}

% 6:50
We wanted to show that this derived category satisfies the analogs of all of these conditions, particularly its closure properties under limits and colimits.
To establish closure under limits, the key step is proving that the category is closed under products.
% 7:05 
A technical issue arose in this proof: that we needed, that, say $(M_\al)_{\al \in A}$ is collection of objects in $\Mod_R$, need \note{todo}

viewed as objects in the derived category concentrated in degree 0, their product in the derived category must still satisfy this condition.
% 7:36
The subtlety here is that while countable infinite products remain exact in our setting of light condensed abelian groups, arbitrary infinite products do not.
% 7:48
As a result, the product functor has right derived functors, and we must ensure that the product remains in $\Mod_R$ for all $i$.

% The key point for proving closure under limits was to show that the product of a family of objects in $R^\tri$, 

 
%still satisfies the condition of lying in $R^\tri$. So the product functor has right derived functors, and we need to ensure that the product lies in $R^\tri$ for all degrees. 
 
% 7:56
This can be proved as follows from the axioms.

Set $M = \bigoplus_{\al \in A} M_\al$.

% 
Alpha, then this guy is a retract of $\R^I$ product $\alpha$ in $M$, because termwise it is obviously this system is a retract of this system here. But this is equivalent to saying that $X$belongs to $\Hom_M(\oplus_I \R, M)$. 

% 8:50
\ques What about products of infinite complexes, unbounded below complexes? 

\ans Yes, the argument was addressed in the last lecture.

\ques Because there are kind of it is possible, so that we can reduce to countable cases? 

\ans Exactly.

% 9:15
\ques I think also that, I don't know, maybe you know the reference. In general, if we have a Grothendieck category and Grothendieck subcategories that are closed under kernel and cokernel  % todo 

 coproducts and filtered direct limits,
% 9:35
Then in the derived category when we consider objects whose cohomology lies in the subcategories, this is also present. This, I think, can be shown. I don't know the reference for this, 

% 9:48
\ans I don't know. 


% 9:50
\ques But this is just an elementary thing. We can do it for complexes, and I think this can be used to give a less... Once we know this, which actually one can formulate in terms of every complex being the limit of smaller ones with some bound, % then this can slide well.

% 10:09
\ans I don't think it's necessary to use this. In fact, we can explicitly construct the left adjoint at the derived level by taking the left derived functors of the left adjoint we already have at the abelian level. Maybe it does not quite work like that, but it is possible. I am willing to believe that the $\infty$-category version can be avoided.

\scholze Let us... We can first... the actually presentable, because we are just... that all the... right, category. % todo


\clausen That is a good argument, because this is not necessarily the derived category of $\Mod_R$. However, what Peter mentioned is that there is a general principle regarding presentable categories being closed under limits and the category of categories, provided we have functors that commute with colimits, and the homology functors also commute with colimits. 
% 11:20
From this, we can conclude that $D(R)$ must be presentable. Then, the $\infty$-categorical adjoint function theorem (the more naive version proved by Lurie) would provide us with the left adjoint.

%  11:36
Let's move on to some real mathematics.

\ques When you did before without light, you did everything... Is there a close relationship between the notions in the light setting and the general one? Specifically, that is, so a condensed ring in the light gives one in the general, and subcategory, subcategory, and so on, maybe, or is there something more complicated involved 

% 12:04
\ans The first statement is entirely accurate: light condensed rings embed fully faithfully into all condensed rings. However, when it comes to the analytic ring structure, it is a bit more subtle.
% 12:18
This is because, a priori, if we have a light analytic ring in the light sense, we have only decided what the free module should be on light condensed sets.
% 12:35
In the previous lecture, we also discussed examples of analytic ring structures in the solid context. 

Let's consider  a solid ring. It is a condensed ring whose underlying condensed abelian group happens to be solid. In this context, we introduced the subset of power bounded elements:
\begin{align*}
R^\tri \in \CAlg(\Solid_\Z) \leadsto R^\circ \subs R(\ast) \text{ power bounded elements}.
\end{align*}

For any subset $R^+$ contained within the power-bounded elements with the additional assumption that $1 \in R^+$), we obtain an analytic ring structure.
\begin{align*}
\text{For any } R^+ \subs R^\circ, 1 \in R^+ \cl \text{ get analytic ring structure on } R^\tri \text{, denoted } (R^\tri, R^+)^\solid.
\end{align*}

% 13:48
As indicated by the notation, the underlying condensed ring is simply our ring $R^\tri$. The category of modules is then defined as follows:
\begin{align*}
\Mod_{(R, R^+)^\tri} = \{ M \in \Mod_{R^\tri} | \IntHom(P,M) \}.
\end{align*}

% 14:40
\begin{remark}
The condition that $1 \in R^+$ ensures that every $M \in \Mod_R$ belongs to $\Solid_\Z$. If we plug in $f = 1$, that was our definition of $\Solid_\Z$. Additionally, the condition $R^+ \subs R^\circ$ guarantees that $R^\tri$ is complete.
\end{remark}

% 15:15
Recall that the interpretation of this second coordinate in the analytic ring structure was that it is specifying a notion of complete modules, adapted to the specific situation at hand. 
% 15:22
We require the ring itself to be complete; otherwise, we would complete it. So that is the condition on power bounded elements. By definition, this translates to saying that the underlying ring itself should be complete, which is one of our required axioms \note{todo, add what he points at, probably axioms at the beginning}.

% 15:40
\begin{remark}
Another point to note is that there is no loss of generality that $R^+$ is an integrally closed subring containing all topologically nilpotent elements. 

% 16:15
We observed that if a solid analytic ring structure on a solid ring \footnote{This means: an analytic ring structure \note{add what he points at } such that every module over it is solid as an underlying abelian group}, then the collection of $f$ for which this is an isomorphism for all $M$ \note{add what he points at} forms an integrally closed subring containing all the topologically nilpotent elements. 

Thus, we can always include these elements $R^{\circ\circ}$, take the subring they generate, and take the integral closure. This does not affect the theory.
\end{remark}

% 16:58
\begin{example}
Consider a Huber ring when those things can always be considered as solid condensed rings, just by taking the associated condensed ring. In this context, the integrally closed subrings $R^+$ are equivalent to the open integrally closed subrings. These subrings are precisely the $R^+$'s in Huber's theory. \note{todo}
\begin{align*}
R^\tri \text{ Huber ring}
\end{align*}
\begin{align*}
R^{\circ\circ} \subs R^+ \subs R^\circ
\end{align*}
% not sure what the integrally closed subrings are here.

\end{example}

% 18:30
In general, for an arbitrary solid ring, the possible choices for $R^+$, when we specialize to the case of a Huber ring, it recovers exactly the choices of $R^+$ we have in Huber's theory. \note{todo, improve phrasing}

% 18:48
From now on, unless specified otherwise, when I refer to a Huber ring, I will mean a complete Huber ring.
% 18:55
\ques Sometimes, people use derived complete things. If we have something that is derived complete, it also seems to give a condensed $\Spec$ object. Is it possible to say that this is also solid? 
%19:10
\ans Yes, there's a fun story with that. Maybe I will cover it later in the lecture. But to answer your question, yes, it is indeed possible.

% 19:25
Moreover, in this case, $R^+$ is recovered by the solid analytic ring $(R^\tri, R^+)^\solid$. Essentially, it is basically to the ...\note{todo}. 
% 19:43
Thus, Huber's pair $(R^\tri, R^+)$ is exactly the same as this solid analytic ring structure:
\begin{align*}
(R^\tri, R^+) \iff (R^\tri, R^+)^\solid.
\end{align*}

In the general case, I did not claim that starting with an integrally closed subring satisfying these conditions and forming the theory, there might for some other reasons also be other things that $f$ that satisfy this property for all our $M$.
However, in the case of Huber rings, we can show that there are not---we did not do it last lecture, but it was done in a previous lecture. \note{todo, clarify this paragraph}

% 20:48
Okay, questions. 

% 21:02
\subsection{Localization}

Now we are going to discuss localization. 

% 
\begin{remark}
Let me make a remark which might seem shocking at first glance, but it is actually trivial. 

If $R$ is a solid ring, and $R^+$ satisfies the following conditions:
\begin{align*}
R^+ \subs R^\circ
\end{align*}
\begin{align*}
1 \in R^+
\end{align*}

You can also assume that $R^+$ is an integrally closed subring containing the topologically nilpotent elements. The condition that defines the analytic ring structure is simply an additional condition imposed on all $f \in R^+$. 

By definition, $R^+$ is a subset of the underlying discrete ring $R$. Thus, all the data used to define the analytic ring structure is already present at the discrete level. 

% 22:10
This gives us another pair
\begin{align*}
(R(\ast), R^+),
\end{align*}

where $R^+$ remains unchanged. In the discrete case, all elements are power-bounded, so they are certainly still power-bounded in this setting as well.
% 22:30
This observation shows that if we are interested in solid modules over our original $\Mod_{(R,R^+)^\solid}$, we can take the ones over where we have a discrete ring. This already captures all  the relevant information about the analytic ring structure:
\begin{align*}
\Mod_{(R,R^+)^\solid} = \Mod_{(R(\ast),R^+)^\solid}.
\end{align*}

% 22:55
All that remains is to observe that $R$ will be a commutative algebra object in here:
\begin{align*}
R \in \Mod_R (\Mod_{(R(\ast),R^+)^\solid}).
\end{align*}

We consider $R$-modules abstractly within this abelian category. 
% 23:15
It is important to note that since we are working with condensed modules, even when we have a discrete ring, we have a huge amount of new modules beside that it discrete modules that we can consider.\note{todo, check}
% 23:20
In particular, over the discrete condensed ring $R(\ast)$, we still have $R$, the genuine condensed ring. The theory for an arbitrary $R$ is, in fact, obtained from from the discrete case via a completely straightforward base change.

\end{remark}

% 24:21
We are now going to discuss localization and how these categories,
\begin{align*}
\Mod_R (\Mod_{(R(\ast),R^+)^\solid})
\end{align*} 

glue together. However, it will actually be sufficient to consider only the discrete case.
% 24:30
The reason is that if we understand how the category $\Mod_{(R(\ast),R^+)^\solid}$ glues, then we can then impose the $R$-module structure on top of it.

This allows us to deduce how the larger category
\begin{align*}
\Mod_R (\Mod_{(R(\ast),R^+)^\solid})
\end{align*} 
glues as well. 
% 24:41
I want to emphasize from the outset that I am discussing only one specific example of gluing. I am not claiming this is the most general kind, but it is nonetheless quite general. You could think of it as a particular approach to gluing.

% 25:00
To draw an analogy, let's consider the familiar setting of discrete rings. If $R$ is a commutative ring, then we have its usual derived category of $R$-modules:
\begin{align*}
R \text{ commutative ring} \rightsquigarrow D(R).
\end{align*}

\begin{remark}
% todo
% 25:35
discrete $R$-modules
\end{remark}

This localizes over the Zariski spectrum of $R$, $\Spec(R)$. I will clarify the precise meaning of this shortly.

% 26:05
$\Spec(R)$ is the set of prime ideals 
\begin{align*}
\{ \text{prime ideals } p \subseteq R  \},
\end{align*}
and there is a basis of quasicompact opens closed under finite intersection.

% 26:15
These are the so-called "distinguished opens". Let us say $U(f)$ are the set of prime ideals $P$ such that $f$ is not in $P$:
\begin{align*}
U(f) = \{ p: f \notin p \}.
\end{align*}

So $f$ maps to something nonzero in the residue field.

% 27:18
There is a structure sheaf, and its value on this distinguished open $U(f)$ is 
\begin{align*}
O(U(f)) = R[\frac{1}{f}], 
\end{align*}

the localization of $R$ at $f$. 

% 27:34
It is important to note that neither $U(f)$ nor $R[\frac{1}{f}]$ determine $f$, but they do determine each other. In fact, $U(f)$ is naturally identified with $\Spec(R[\frac{1}{f})$, and this matches up the distinguished opens:
\begin{align*}
U(f) = \Spec(R[\frac{1}{f}).
\end{align*}

% 28:30
If $U$ is a distinguished open set, we can associate to it the derived category
\begin{align*}
U \text{ dist} \leadsto D(\O(U)).
\end{align*}

% 28:41
If $V \sube U$, we obtain a base change functor:
\begin{align*}
V \sube U \leadsto \text{ base change } D(\O(U)) \to D(\O(V)).
\end{align*}

The following theorem, which is entirely classical, states that this presheaf is, in fact, a sheaf of $\infty$-categories.

\begin{theorem}[Lurie]
This presheaf is a sheaf of $\infty$-categories.
\end{theorem}

% 29:17
In this setting, we also obtain a sheaf of abelian categories. In fact, we could have formulated same story with mod the usual abelian... (points to $D(\O(U))$)

% 29:24
\ques ... category \note{did not hear this well}

\ans Yes. 

% 29:30
\ques ... sheaf, hypercover 

% 29:38
\ans Yes, that is right.

% 29:38
\ques So this is proved in Lurie somewhere?

\ans I assume it is proved in Lurie somewhere, yes.

\ques But ... sometimes don't know ...

\ans In this language, I am sure it is Lurie, yes. I will explain the argument in a second.

% 30:00
In this case, we also have a sheaf on the level of abelian categories, and even more as Gabber said hypersheaf. 

\begin{warning}
% 30:09
In the setting we are about to discuss, we obtain a sheaf only at level of the derived category. However, we do \emph{not} get a sheaf at the level of abelian categories.
% 30:21
The core issue, which also appeared in my previous lecture. So in this situation \note{todo}, the localization maps are flat.

Specifically,
\begin{align*}
\O(U) \to \O(V)
\end{align*}
is a flat map, as it is simply a localization.
% 30:39
Thus, when working with derived statements, it is not too difficult to deduce corresponding abelian statements using the flatness of localization. However, in the setting we are about to discuss, these localization maps will \emph{not} be flat in general.

\end{warning}

\begin{example}
% unlike in the discrete case.
% 30:33
One example of such a localization map is going to be this localization from the affine line to the closed unit disk, which was this $t$-solidification. I previously mentioned that $t$-solidification is not precisely $t$-exact; there is a discrepancy by one. 
% 31:08
This discrepancy is what obstructs the sheaf condition at the level of abelian categories.

\end{example}

% 31:14
\ques ... closely related statement ... statement, that the derived category .. ring, is the same as the quasicoherent derived category ... $\Spec$ of the ring. In general, I think ... that in some bounded proved ... stacks project.
% 31:32
Then there is another statement about sheaf property, which I also do not refer, but I think ...
If you have any thing ... topos
derived category of this $U$. This should also be a hypersheaf. Is it? So is this also in Lurie?

% 
% So, this is in $L$ also. First of all, what I said should be correct, I think. 

% 31:10
\ques Given any topos, associated to do you have any object, and you have any object in the right category of the topos restricted to $U$? \note{todo}

\ans Yes.

\ques Is this a hypersheaf?

\ans It is not a hypersheaf, but it is a sheaf.

\ques ... \note{todo}

% 32:26
\ans Why would it automatically be a hypersheaf?

% 
\gabber I think I was able to do it in a more classical formulation, but it is a... Is it. What do you know about this statement?

\ans Oh, the usual derived category? Maybe it is a hypersheaf, yes. I don't know.

\ques You don't know the reference?

\ans Well, I don't know the exact statement. Now that I think about it, a hypersheaf does sound plausible. But I'm certain that Lurie proves it's a sheaf. As for hypersheaf... Maybe, I'm not sure.

\ques Does Lurie prove this in one of his books?

% 33:00
\ans Yes, I'm sure.

% 33:303
\ques I also came across something you mentioned in a text I found online. Instead of looking at the derived category, we could look at Fun$_\infty$ or $\Sh$ and this would be equivalent to the derived category of the hypersheaves. 

% 
If we do hypersheaves with values in the derived category of abelian groups, that is the same thing as the derived category of the category of sheaves of abelian groups. I assume this is also proved somewhere in Lurie. 

I would like to move on. Maybe we have this discussion at another time. 

% 33:50
So, that is one part of the analogy. The second part involves the pair $(R, R^+)$, a discrete Huber pair. This simply means that $R$ is an ordinary commutative ring, and $R^+$ is an integrally closed subring.

% 34:35
Then we have assigned to this pair the solid category $D((R,R^+)^\solid)$. The claim is that this localizes on something else---the valuative spectrum of the pair $(R, R^+)$, denoted $\Spv(R, R^+)$. So, what does this mean?

% 35:06
The set of prime ideals serves as a way to identify where functions vanish or do not vanish.
% 35:18
In this context, we can think of it as a binary condition: whether a funtion is zero or nonzero. 

% 35:26
In the valuative spectrum, we are allowed some more refined information. This is not just information about whether a given function vanishes or not, but we can also compare two functions to see if one is greater than the other.
% 35:39
We can measure this comparison through a valuation, which is a function $v \cl R \to \Gamma \cup \{0\}$, where $\Gamma$ is an ordered abelian group written multiplicatively.
\begin{align*}
\Spv(R, R^+) = \{ v \mid v \cl R \to \Gamma \cup \{0\} \}
\end{align*}

% 36:01
The axioms for a valuation include multiplicativity: $v(fg) = v(f)v(g)$, and the obvious rule that $v(0) = 0$ for any function.
% 36:13
There is also the non-archimedean condition: $v(f+g) \le \max\{v(f), v(g)\}$. 

Additionally, we have the basic values:
\begin{align*}
v(0) = 0, v(1) = 1.
\end{align*}

% 36:36
Next, we consider the subring $R^+$, and require that all elements in $R^+$ are integral with respect to the valuation:
\begin{align*}
v(R^+) \leq 1.
\end{align*}

% 36:49
There is an equivalence relation on valuations. For instance, we can always enlarge the ordered abelian group in an arbitrary way, without fundamentally changing what is going on.
% 37:02
One way to describe this equivalence relation is as follows:
\begin{align*}
v \sim w \iff \forall f, g \in R, v(f) \leq v(g) \iff w(f) \leq w(g).
\end{align*}

% 37:24
In other words, the key aspect of a valuation is the binary relation it defines on functions, which is testing whether one is greater than the other.

% 37:32
\ques Is it the same as $\Spa$ in this case, in the sense...?

% 37:67
\ans Yes, it is also $\Spa$, because the continuity condition is trivially satisfied when the ring is discrete. 

% 37:48
That is a bit of a mouthful if you have seen it before. There is  another way to look at this that maybe explains exactly in what sense it is larger than $\Spec(R)$ is.
% 38:00
Essentially, it's equivalent to a pair of a prime ideal and a valuation subring, where $A$ is a valuation domain in the residue field:
\begin{align*}
(\cP, A \sube K(\cP))
\end{align*}
% 38:59
\note{todo, I don't understand the correction made at 38:59}

% 38:20
We have the information which records whether a given function vanishes or not. Additionally, we introduce an extra piece of data $A$, which specifies when a function---or a fraction of functions ($K(\cP)$), where the denominator does not vanish---should satisfy the condition $\leq 1$. 
% 38:36
This condition is essentially equivalent to the one described earlier.

% 39:10
The key point is that by working within a much larger category, we gain greater flexibility in how we perform localization. This approach connects with this classical discussion of valuations. 

\begin{example}

If you have never seen this before, a good example to look at is $\Q$. 

% 39:30
You might be familiar with the classification of valuations there. There is the trivial valuation, which corresponds to equality in this case ($A = K(\cP)$), where for every prime ideal, we have the trivial valuation---zero if the element lies in the prime ideal and one otherwise. Additionally, for every prime $p$, we have the $p$-adic valuation.

% 39:53
For the generic point of $\Spec(\Z)$, we have the special points of $\Spec(\Z)$. But then we have these things in between, which are nearby $p$ but not equal to $p$, these $p$-adic valuations.

Sorry, I was referring to $\Z$ here, not $\Q$. But the fact that we can classify these valuations can be a bit misleading, because once we introduce an extra variable, then all of a sudden things explode.

% 40:20
There are many different types of valuations, because in a surface, we can have lots of different kinds of curves passing through a given point. We have valuations of so-called higher rank, which add significant complexity to the theory. 

\end{example}

% 40:35
I am not going to go too much into this, but I will stick to mostly formal aspects for now. Let's continue with the table of analogies.

% 40:46
\note{Point at $U(f) \sim \Spec (R ...)$}
We considered $\Spec(R)$, where we had a particularly nice basis for the topology: quasi-compact open sets, closed under finite intersections. Each of these sets was also of the same form as the global one, but with a different input datum, $R[\frac{1}{f}]$. 
The situation is analogous here. We have a basis of quasi-compact open sets, also closed under finite intersections. These sets are referred to as the \emph{rational opens}. 

% 41:24
They depend on the choice of certain elements in our ring. We select arbitrary $f_1, \ldots, f_n, g \in R$ from the ring. Then, we can define the following set:
\begin{align*}
U(\frac{f_1, \ldots, f_n}{g}).
\end{align*}

This is the set of valuations $v$ satisfying all of these conditions, such that moreover $v(g)$ is non-zero, and $v(f_i) \leq v(g)$ for all $i$:
\begin{align*}
\{ v | v(g) \neq 0,  v(f_i) \leq v(g) \forall i \}.
\end{align*}

% 41:52
In a certain sense, it resides within the distinguished open set, the Zariski open, given by the condition $g \neq 0$. We then make use of the additional flexibility, of \note{todo} we can also impose inequalities.
% 42:02
Thus, by slightly shrinking this Zariski open using these inequalities, we still obtain an open subset.

% 42:40
There is a structure sheaf, but more precisely, there are structure \emph{sheaves}. On this $\O(U(\frac{f_1, \ldots, f_n))}{g}$, we have one thing which just takes the Zariski localization. 
\begin{align*}
\O(U(\frac{f_1, \ldots, f_n}{g})) = R[\frac{1}{g}]
\end{align*}

% 42:57
But then we also get a choice of integral elements. It is going to be a subring of $R[\frac{1}{g}]$. We get it by taking the integral elements we had before, or rather their image in there, and then adjoint, and then looking also at these elements $\frac{f_1}{g}, \ldots, \frac{f_n}{g}$. That might not be integrally closed, so we take the integral closure. 
\begin{align*}
\O^+(U(\frac{f_1, \ldots, f_n}{g})) = R^+[\frac{f_1}{g}, \ldots , \frac{f_n}{g} ]
\end{align*}
\begin{align*}
\sube R[\frac{1}{g}]
\end{align*}
\note{todo, integral closure}

% 43:31
Essentially, we conside all of the elements which the valuations in our open subset think should be $\leq 1$. For $R^+$, this is already given by definition, and we have enforced it for the elements $\frac{f_i}{g}$. The collection of these elements forms an integrally closed subring, and we have this property for all such objects.

% 43:52
Moreover, we again encounter a nice recursive property: $U(\frac{F_1, \ldots, F_n}{G})$ is equivalent to the valuative spectrum of $\O_U^+$.
\begin{align*}
\O(U(\frac{f_1, \ldots, f_n}{g})) \equiv \Spv(\O(U), \O^+(U))
\end{align*}
This matches up with the rational opens.

% 44:20
Here is another place we can see the necessity of including the data of $R^+$ in the general theory. One could have argued, "I want a larger space, so I'll omit the condition $v(gf) \leq 1$ for all $f \in R^+$. Why is this necessary?"

% 44:37
However, by defining these rational open subsets, they will no longer take the form $\Spv(R,R^+)$, for some ring $R$, because you have enforced the condition that $\frac{f_i}{g} \leq 1$. 
% 44:50
The abstract ring $R[\frac{1}{g}]$ \note{todo: add ref} does not  retain this information. Therefore, if we want this condition that the rational opens themselves should be of the same form as the global object, it is essential to include the extra data of $R^+$ from the outset.

% 45:06
\ques Of course, this ... \note{todo}

% 45:23
\ans There is a continuous map from this to this \note{todo}.
\begin{align*}
\Spv(O(U), \O^+(U)) \to \Spv(R,R^+)
\end{align*}

% 45:33
It induces a bijection. Say, the pullback induces a bijection between rational opens here, which are contained in this open subset \note{todo} $\O(U(\frac{f_1, \ldots, f_n}{g}))$ and rational opens in here $ \Spv(\O(U), \O^+(U))$.

\ques \note{todo}

% 46:10
To $U$ a rational open, we can attach $D(\O(U), \O^+(U))^\solid$: \note{todo}
\begin{align*}
\leadsto D(\O(U), \O^+(U))^\solid
\end{align*}

% 46:30
Then if $U \sube V$ inclusion, 

% 
Let me put this: If $U$ is contained in $V$, then we get the pullback map. \note{todo, equations}

% 46:55
In fact, it comes from a map of analytic rings from $\O(U)$ to $\O(V)$ in the sense of the previous lecture. 
\begin{align*}
( , )^\solid \mapsto ( , )^\solid
\end{align*}

% 47:25
So we have a map of condensed rings, which is in this case a map of discrete rings, such that if we have a complete module here \note{RHS}, then when we restrict scalars, it is also complete here \note{LHS}. 

% 47:36
That is the kind of forgetful functor, and then that always has a left adjoint, which is this base change functor. We get it explicitly by taking our module here \note{LHS}, abstractly tensoring up from this ring to this ring, and then re-completing in this theory here \note{RHS}. That is the base change functor.

% 
\begin{theorem}
The theorem---
This presheaf (over there)s is a sheaf of $\infty$-categories.
\end{theorem}

% 48:30
\begin{warning}
This is not true at the abelian level, in contrast to the classical case. These pullback functors are not $t$-exact in general, because the pullback involves a $t$-solidification. As mentioned, this is not a flat operation. 
\end{warning}

% 49:00
\ques Does it have bounds.. cohomological dimension? \note{todo}

% 49:05
\ans Yes, it does. It will be bounded by $n$ \note{add what he points at}. 

% 49:15
\ques The solidification is bounded by $n$ ---I mean, the homology is zero up to $n$.

% So I think we will take a 5-minute break before I get to the proofs. 

\ques Is the proof complete or not?

% 49:37
\ans Probably not in general, but there is an abstract result that if the space has finite Krull dimension, then hypercompleteness is automatic. This is sometimes useful.

% 49:52
\ques If we start with a solid ring, we can take its underlying set, which is discrete. We mentioned it is the same as the module versus ... \note{todo} . Is there a case where this is actually idempotent of this ring \note{todo} .. algebra?

% 50:10
\ans I don't think so. For example, $\Z$. 
\begin{align*}
\Z \mapsto \Z[[T]]
\end{align*}

\ans We demonstrated that the $\Z$-power series $T$ is idempotent over $\Z[T]$, but this discrete ring is too large for that to hold in general. This is not going to be idempotent; there is no extra reason why it should. While I haven't considered it in detail, I would expect the answer to be no.

\subsection{Proof}

% 50:50
We have stated the theorem, and now we aim to explain the proof. To motivate it, we will give a certain proof of this classical theorem here \note{add what he pointed at}. 

% 51:08
In the classical case, there are many different possible arguments for this, especially since these localizations are flat, there is lots of flexibility in how we set things up.

% 51:18
But we want to describe a particular argument for this claim here \note{add what he pointed at}. This argument will translate without too much difficulty to this case here \note{add what he pointed at}. 

% 51:58
There is one remark that could be made in both settings, which we forgot to mention.

% 52:05
We defined a sheaf of $\infty$-categories on the rational opens. While not every open subset is rational, the rational opens form a basis for the topology. There is a general result that when we have a basis for the topology closed under finite intersections---this condition being essential in the $\infty$-context---a sheaf on that basis uniquely extends to a sheaf on the entire space in the naive manner: by taking limits over arbitrary opens.

% 52:40
We are currently only describing the sheaf of categories on the rational opens. However, after the fact, we also obtain a category associated with an arbitrary open subset, whether or not it is rational.

\begin{sketch}
% 52:55
We sketch the proof of the classical theorem, what one might call Zariski descent, for $D(R)$. We are interested in the site of distinguished opens inside $\Spec(R)$. 

% 53:20
So we consider this site of distinguished opens $U \subs \Spec(R)$, equipped with an open cover topology. We have a reasonable understanding for distinguished opens to form a cover. For a ring $R$, the distinguished opens cover $\Spec(R)$ if and only if we are inverting some elements, such that these elements generate the unit ideal.

% 54:00
However, through a series of reductions, we can show that it suffices to check the sheaf condition for a very specific case of such a situation.

% 54:08
So the lemma is as follows:

Lemma: The Grothendieck topology is generated by covers of the following form.
Take a distinguished open $U \subs \Spec(R)$, let $f \in \O(U)$, and form a cover, which is \note{todo, diagram}
$U(f)$ and $U(1-f)$ covering $U$.

% 54:59

\end{sketch}

% 55:09
This is a very simple example of two elements, $f$ and $1-f$, which generate the unit ideal in the ring. The claim is that to verify something as a sheaf, it suffices to check the sheaf condition in this one specific situation. 

% 55:18
\gabber This was originally in Quillen's proof of the---anyway, it is easy, but there was something of Quillen and he proved the Serre conjecture.

% I think Ofer Gabber is referring to the Quillen–Suslin theorem
% Quillen, “Projective Modules over Polynomial Rings” (Invent. Math. 1976)
% https://en.wikipedia.org/wiki/Quillen%E2%80%93Suslin_theorem
% https://ncatlab.org/nlab/show/Quillen-Suslin+theorem

% 55:32
\gabber Specifically, he wanted to prove that if we have a vector bundle on affine space over a ring derived over a local ring, then it is extended from the base ring. His proof reduced to problem to this situation, and it was a bit trickier.

% 55:42
\clausen Quince is a clever guy, so let us give the proof.

% 56:00
We can describe the cover algebraically. In general, the covers would be described like this: we take $f_1, \ldots, f_n$ in $\O$ generating the unit ideal, such that there exist $x_1, \ldots, x_n in \O(u)$ such that
\[
x_1 f_1 + \ldots + x_n f_n = 1
\]

The general cover is the $U(f_i)$.

% 56:35
Oh, I can't believe I didn't think of that, 


\gabber will not be okay, because we cannot generate the empty set from non-empty things.

% 
But plus empty cover of empty set. 

% 57:02
Verifying the sheaf condition in this case amounts to checking that the value of our sheaf on the empty set is the terminal object in the target category of our sheaf. Usually, that can verified without much difficulty.

% 57:28
But note that this cover here \note{todo, add what he points at} is refined by another cover obtained by multiplying each $f_i$ with the corresponding $x_i$. 
\[
\{ U(f_i x_i)  \}_{i \in I}
\]

% 57:47
Each $U(f_i x_i)$ is a smaller distinguished open, yet the collection still generates the unit ideal because of the same expression.
Then we can assume that 
\[
f_1 + \cdots + f_n = 1.
\]

From here, the argument proceeds by induction on $n$.

% 58:17
Suppose we have $f_1 + f_2 + f_3 = 1$ and we want to check the sheaf condition. We can localize to $U()$ \note{todo}, since we assume the sheaf condition holds for covers of this form. It is enough to check the sheaf condition when we do these localizations. But when you localize to $U(f_1 + f_2)$ \note{not sure he point to this or $U(f_3)$}

when $f_1 + f_2$ is equal to a unit, multiplying by the inverse of the unit, you reduce to that case there. Then, when $f_3$ is a unit, there is something similar.

% 59:05
\gabber First there is a statement about the generation of the product \note{not sure if product} topology. (...)
Then there is another statement, which is probably ... under some conditions that it is enough the check the sheaf condition on all generators of, on coverage to generate topologyand then base change it.
Of course, assuming that things like the intersections exist.

% 59:27
\clausen Yes, exactly, but I chose this collection to be compatible under base change. This collection here \note{todo, add what is pointing at} is closed under base change. That is an important technical point, when we get into making this argument completely precise. 
I have set it up to ensure this property holds. .. 
% 59:44
When we restrict our cover to thisn the cover is split, because this was one of your covering elements. So the sheaf condition is automatic here. The sheaf condition here follows by induction. That is essentially the core of the argument.

% end proof

% 1:00:23
What about this case here \note{todo}?

% 1:00:32
What we are trying to show, checking the sheaf condition for ... Well, it is a distinguished open in an element $f \in R$, but now there is no loss of generality assuming $U = \Spec(R)$.

\note{todo diagram 1:01:00}

% 1:01:04
What is the sheaf condition in this case? In this \note{add what he points at} case, we have just two elements ($U(f)$ and $U(1-f)$), and then we have their intersection. The sheaf condition says that if we look at the derived category of $R$, and the derived category of $R[\frac{1}{f}]$ and the derived category of $R[\frac{1}{1-ff}]$, and then the derived cateogry on their intersection, which is where you invert both, this should be a pullback of $\infty$-categories.


\note{todo diagram 1:01:25}

% 1:01:38
Let me pause here, as I haven't spent much time discussing what this notions mean, such as sheaves of $\infty$-categories, limits of $\infty$-categories, and so on. However, I can presents a completely (so to speak) elementary case of these pullbacks, to give us a better intuition for the claim.

% 1:01:55
Claiming that this \note{add ref to diagram} is a pullback of $\infty$-categories, what does that mean concretely? The functor from $D(R)$ to the pullback category 
\begin{align*}
D(R) \to D(R[\frac{1}{f}]) \times_{D(R[\frac{1}{1-f}])} D(R[\frac{1}{1-f}])
\end{align*}

should be an equivalence of categories. So, how should we interpret this $\infty-$category \note{points to RHS}? We start with an object in the derived category $D(R[\frac{1}{f}])$ and an object in the derived category $D(R[\frac{1}{1-f}])$. Then, we  introduce additional data---an isomorphism between these objects \note{todo, what he points at, it is the cross produt}. However, this is not an isomorphism in the usual derived category, but rather an isomorphism in some $\infty$-category version. 

% 1:02:50
For example, imagine that $D(R[\frac{1}{f}])$ is represented by a complex of projective objects, and $D(R[\frac{1}{1-f}])$ is represented by a complex of projective objects as well. In this case, we would want to construct a chain homotopy equivalence between their images there. For simplicity, let's assume they are bounded above. From this, we can build an $\infty$-category. We then define a notion of chain homotopy within this context, and so on.

% 1:03:14
\emph{Essential surjectivity} means that we can glue objects together in the derived category. 
If we have a chain complex in $D(R[\frac{1}{f}])$, and a chain complex $D(R[\frac{1}{1-f}])$, along with an explicit identification between them \footnote{So maybe we choose some quasi-isomorphic models and make a chain homotopy equivalence between them.} \note{todo, add what he points at}, then this collection of data uniquely comes from an element here, up to quasi-isomorphism. 
% 1:03:40
The key point is that we must specify the data of the chain homotopy equivalence here \note{todo, add what he points at}, to obtain a well-defined object there \note{todo, add what he points at}. This is what we mean by essential surjectivity.

% 1:03:50
\emph{Full faithfulness} describes a different condition. It says that if we have two objects in $D(R)$, and we want to know the $\Hom$-spaces between them \footnote{For example, we can compute $\Ext$ groups, or the $\RHom$-spaces between two objects.}, we can get it by base changing here $D(R[\frac{1}{f}])$ and taking $\RHom$'s, base changing here $D(R[\frac{1}{1-f}])$ and taking $\RHom$'s, and then taking the homotopy pullback of those complexes for $\RHom$'s we have there ($D(R[\frac{1}{f}]) \times_{D(R[\frac{1}{1-f}])} D(R[\frac{1}{1-f}])$). This is the same thing like a shift of a mapping cone of some direct sum of these two mapping to that.

% 1:04:18
\ques But is it equivalent to like the derived category of this diagram? You can say, on the abelian level, modulo a diagram is just giving a modulo over every ring. And maps ... then you take the derived category .. something condition homology?

% 1:04:33
\ans It sounds reasonable, but I am not entirely sure. That is certainly not how I think about it.

% 1:04:48
So that is how to think about this result \note{add ref}. It allows us not only to glue objects that are defined locally in a derived sense, but also to perform global $\Ext$-calculations by localizing.

% 1:05:05
\begin{question}
How do we formally prove such a statement?
\end{question}

\begin{sketch}
Note that each base change functor has a right adjoint, which is the forgetful functor
\begin{align*}
D(R[\frac{1}{f}]) \to D(R).
\end{align*}

That is for each of the individual maps in diagram \note{todo, add ref}.
From this, it follows formally that this \note{todo, add ref} functor also has a right adjoint.

% 1:06:02
We can explicitly describe the right adjoint. Suppose we have a triple $(M, N, \alpha)$, where 
\begin{itemize}
	\item $M$ is a module in $D(R[\frac{1}{f}])$,
	\item $N$ is a module in $D(R[\frac{1}{1-f}])$, and
	\item  $\alpha$ is an isomorphism between their common base changes:
	\begin{align*}
	\alpha \cl M[\frac{1}{1-f}] \equiv N[\frac{1}{f}].
	\end{align*}
\end{itemize}

% 1:06:24
Then we apply the right adjoints to each of these objects \note{todo, add what he points at}, and then we take a limit. 
So we take $M$ cross $N$ with $M[\frac{1}{f}]$ , which is the same thing as $N[\frac{1}{1-f}]$.
\begin{align*}
M \times_{M[\frac{1}{f}]} N = M \times_{M[\frac{1}{1-f}]} N
\end{align*}

\end{sketch}

\ques \note{Can't hear a question/remark}

% 1:06:50
\ans Yes, you have to open up Lurie to find the precise version. But it works.

% 1:07:02
\ques The trick to get used to conversing by two op is just to have an easier diagram?

\ans Yes. In principle, we could also do this argument without doing the reduction, that is correct. But it is certainly easier to talk about it this way.

\gabber Because ... finitely many intersections, just to ... like alternating circles ... finite \note{can't hear this question/remark well}

% 1:07:32
\ans I think that once we reach the statement we are trying to prove with the valuative spectrum, then we really probably do not want to do it this way. Maybe we could organize it cleverly, but I think doing the reductions makes it much easier.

% 1:07:50
This is good news. This is the great thing about proving something as a sheaf of categories. We automatically obtain a candidate for the inverse functor, namely the right adjoint. So we have a functor we want to prove is an equivalence. 

% 1:08:05
Having a right adjoint means that we obtain both a unit and a counit of the adjunction, and to prove equivalence we need to verify that each of them is an isomorphism. Then we need to check, one of them will be a map in this category, and one of them will be a map in this category \note{todo, add categories}.

% 1:08:30
\begin{example}
For example, for the unit, we need that if $M \in D(R)$


This is a pullback. That would be the claim that the unit of the adjunction is an isomorphism.

% 1:09:02
This follows from the statement that if you have any element, call it $N \in D(R)$, such that .. \note{todo} then $N = 0$.

% 1:09:22
So you want to test something as a homotopy pullback, you can measure the difference between this \note{todo} and the homotopy pullback by a mapping cone. That object, you can check with this \note{todo}, will satisfy this condition. Then you want to conclude that the object is zero. 

% 1:09:45
For example, now you could reduce to the abelian level, because an object is zero if and only if its homology is zero, and these are localizations that are flat. So it reduces to the same claim on the abelian level, which is kind of very easy to check. 

% 1:10:00
That was for the unit. Now the co-unit.
It actually reduces to the exact same claim again. So that is kind of the proof. 
% end proof

But I want to point out that what is formally used is ...

% 1:10:20
\gabber So we use localization by $f$ somehow, and this right adjoint commute, that is...

\end{example}

\begin{remark}
Formally, what is used is the following:
\begin{enumerate}
\item Each base change is a localization, so the right adjoint is fully faithfull.
\item The localizations commute with each other. \note{todo, add them} I'll say what this means. It means that if you take something which is local with respect to say this operation, so something on which $1-f$ acts invertably \note{todo}, and then you invert $f$ on that. That $1-f$ will still act invertably. These are things which are all just obvious in this context, but I want to point out exactly what is being used in this argument.
\item If $M \mapsto 0$ on each element of the cover, then $M = 0$. 
% 1:11:50
So if you have a pullback diagram satisfying these three properties, then you are going to automatically get this limit, I mean a square like this, satisfying these three properties. Then you are automatically getting a pullback.
\end{enumerate}
\end{remark}

% 1:12:52
So then the proof of the solid analog. 

\gabber We use the fact that when we invert $1-f$, this is the intersection of the two. 

\answer Right.

\gabber That is, those which are---

\answer Yes, we want to know that when we pass to right adjoints, this thing \note{todo} is just the intersection of those two things as well. Maybe I should have added that to the list.

\gabber We have to understand it in a sensible way... 

% 1:13:30
\answer I guess you are right. 

\gabber Of course, we have to know the language of Lurie to make it precise. 

\answer Yes.

\gabber So it is not...

\answer Well, I mean, the right adjoints are fully faithful, so it is just kind of an object-wise condition you could say, just---yeah.

% 1:13:52
What is the analog? Again, we have the site of rational opens $U$ in this valuation spectrum $\Spv(R, R^+)$, and the Grothendieck topology, open covers.

% 1:14:13
Then we have a lemma that states that this topology is generated by the empty set cover. 
For all rational opens $U$ and all $f \in \O_R$, we have to take care of two different kinds of covers. 

\begin{lemma}
The topology is generated by:
\begin{itemize}
	\item the empty cover of empty set
	\item for all rational opens $U \subs \Spv(R,R^+)$, all $f \in \O(R)$ 
\end{itemize}
\end{lemma}

% 1:14:42
Now we have to take care of two types of covers.
We have:
% todo: diagram
% 
So, we have $U$ covered by $\frac{1}{f}$ and $U \setminus V(f)$. 

\gabber geometry that you have those two 

\answer Right, so the way you read $U(\frac{f}{1})$ is $f$ less than or equal to 1, so $f$ should be integral.
The way you read $U(\frac{1}{f})$ is $f$ is nonzero, but even more, it is $f$ is bigger than or equal to 1, so it's... well away from zero, it is complement of the open unit disk.

% other diagram

% 1:15:18
This $U(\frac{1}{f})$ is again, $f$ is nonzero, and you require $f$ to be bigger than or equal to 1. Similarly here $U(\frac{1}{1-f})$. So this is actually a \emph{refinement} of the Zariski cover we had previously, where we just inverted $f$ and $\frac{1}{f}$, and that was a cover.
% 1:15:52
This is a smaller, a refinement of that, which still covers.

% 1:15:58
\gabber The last thing I think---one, it is enough to do it when $f \in R^+$. 

\answer Okay. I do not think that will be helpful, but that is nice to know. 

% 1:16:06
\gabber Yes, it is like in rigid geometry, in Tate's original work \note{todo, add ref}, where he proved a simplicity theorem. He did not have rational domains, but he has two types of covers, for which we can prove acyclicity. It turns out that it generalizes to the adic case.

% 1:16:30
\answer Yeah. Maybe in the Tate setting, one can arrange that $f$ is ...
 So, but it is---Uber, there is maybe a statement.

The Weierstrass is just $f$ . 
% 1:16:55
I think that in Huber , okay, I also wrote some letters to other people, some to explain.

% 1:17:03
\answer Okay.

I will not give the argument for this. It is a bit more complicated, because the valuation spectrum is more complicated than $\Spec$. But the idea is somewhat similar, you could say. But it is actually a somewhat complicated argument. 

% 1:17:24
\gabber Huber, there is maybe a statement that it is enough to have a rational, so that you have a rational $f$ ... generating the unit ideal and then you... It is enough to check for those.

% That it is enough to have a rational $f_1, \ldots, f_n$ generating the unit ideal. Then we can check just for those. ,

\answer Yes.

\gabber So we can do some little bit of work to reduce to...

\answer Exactly.

% 1:17:42
% begin proof
So, based on wat Gabber said, Huber shows that every cover is refined by one of the following form: take $f_1, \ldots, f_n$ generating the unit ideal, and consider the cover
\[
\Bigl\{\, U\!\left(\tfrac{f_1, \ldots, \widehat{f_i}, \ldots, f_n}{f_i}\right) \Bigr\}_{i \in I}.
\]

In other words, we omit $f_i$ from the numerator and place it in the denominator. 

This collection of opens refines the usual Zariski cover we get when $f_1, \ldots, f_n$ generate the unit ideal, but one can check on valuations that it still covers the valuative spectrum. 

% 1:18:25
Then we do something similar to what we did previously. With this \note{todo} here, using these covers which refine all Zariski covers, we can assume one of the elements is equal to 1. Repeating this reduction iteratively, we eventually obtain the desired thing.

So now what are we reduced to, analogous to there? 

% theorem, reduces to

% 1:19:00
If $(R, R^+)$ is a discrete Huber pair and we take $f \in R$, then we need that...

% 1:19:40
There were two different kinds of covers: $U(\frac{f}{1})$ and $U(\frac{1}{f})$.
% 1:19:55
I will do that one \note{todo} first. 
\[
D(R, R^+)^\solid \eqv D(R[\frac{1}{f}], R^+[\frac{1}{f}])^\solid \times D(R,R^+[f])^\solid
\]

\note{todo, tildes}

$, R^+ + fR^+)$ is just the $T$-solidification, from $\Z[T] \to R$ with $T \mapsto f$. 

The other one

\note{todo}

% 1:21:00
So we are going to just verify that all of these conditions hold. So what is this?
% 1:21:25
There is really only two types of covers here, if you look at it. There is a cover $U(\frac{f}{1})$ and then there is the cover $U(\frac{1}{f})$.
This \note{todo} is of the form $U(\frac{1}{f})$, where $f$ is just $1-f$.
% 1:21:42
So $f \in R$... So what is this?
\[
D(R,R^+) \to D(R, R^+[f] ?
\]
\todo{integrally closed}
% 1:22:00
This is just $T$-solidification for
\[
\Z[T] \xrightarrow{T \mapsto f} R.
\]

% 1:22:23
By definition, $D(R,R^+)$ and $D(R, R^+[f]$ \note{todo} are both analytic ring structures on the same ring. The only difference is that in the second case, we have enforced the extra condition that $x \in R^+$ is solid in $R[\frac{1}{f}]$. This gives us the category we want.


if you enforce this, you also have that condition, not just for $R^+$, but for $f$, but then we said you automatically then get it for the integrally closed subring generated by those, so you get exactly this condition here.

% 1:23:00
So the category that you are getting here is axactly this condition here and the left adjoint is just the $T$-solidification. 
% 1:23:12
Recall that this was given by some $\RHom$
\[
= \IntRHom_{\Z[T]} (,-)
\]

% 1:23:28
That was the first type.

% mini QA
I am doing $U(\frac{f}{1})$.

% 1:23:40
Then there is the one where $U(\frac{1}{f})$. That gives $D(R, R^+) \to D(R[\frac{1}{f}], R^+[\frac{1}{f}] )$. \note{todo, integrally closed}. 

% 1:24:00
This is first inverting $f$, then $T$-solidifying for
\[
\Z[T] \xrightarrow{T \mapsto \frac{1}{f}} R[\frac{1}{f}].
\]
 
The modules here \note{todo} are a full subcategory of $R[\frac{1}{f}]$-modules which is the full subcategory of $R$-modules and the condition is F x  invertably and you have this extra thing that is supposed to be solid.

% Remark
% 1:24:35
But now let me make a remark about that second situation.

% 
I claim that this entire process of inverting $f$ and then solidifying with respect to $\frac{1}{f}$ (this base change) is also described by just an $\RHom$. 

Now I am going to take and take
\[
\Z[T] \xrightarrow{T \mapsto f} R
\],
with $T \mapsto f$, not $\frac{1}{f}$.
% 1:25:06
Now I am going to take $\RHom_{\Z[T]}(-, R)$.
\[
\RHom_{\Z[T]}( \Z[[T]] / \Z[T], -).
\]

Id est, based on the formalism from two lectures ago \note{todo, change this in a ref}, this is the localization which \emph{kills} the idempotent object $\Z[[T]] \in D (\Z[T], \Z)^\solid$.

$\Z[[T]]$-modules and $\Solid_\Z$-modules, we said that this was idempotent. I also said that when you have an idempotent algebra, then when you can kill it by just taking the homotopy fiber of the unit map \note{points to quotient} hitting that thing and doing this $\RHom$-formula.

% 1:26:10
\gabber ... \note{todo} This is what you call ..

\ans This would be the same thing as $D(\Mod_{\Z[T]}(\Solid_\Z))$. So, this is kind of the new notation fitting it in the general framework. 

\gabber Because before, for Huber pairs, we wrote the $D(R, R^+)$, you did not write solid.  No, you had integrally closed $R$ ... \note{todo}

% 1:26:50
\ans Let me explain what the point is here. This \note{todo ref} localization was supposed to be given by first inverting $F$ and then doing the solidification. 
But again, this solidification---the first claim is that this functor \note{todo ref} already inverts $f$. 

% 1:27:05
If we have something $f$-torsion, it is going to be killed by this \note{todo ref}. The reason is we are killing this whole guy $\Z[[T]]$. Therefore, in particular, we are killing any module over this object. But everything $T$-torsion is a module over $\Z[[T]]$.
% 1:27:23
So this automatically inverts $f$, because anything $T$-torsion is a $\Z[[T]]$-module.

% 1:27:42
If we have a solid Abelian group, which is a filtered colimit of things killed by powers of $T$, then it is a $\Z[T]$-module. 
% 1:27:50
That is just a condition (closed under limits and colimits), so we can reduce to checking for something which is uniformly killed by some power of $T$. But then it is obviously a $\Z[[T]]$-module, because it is a module over the truncated power series ring.

% 1:28:10
Then it would be the same thing to write this formula \note{todo ref} where we invert $T$. But after a change of variables, it is exactly the same thing as $T$ inverse-solidification as described by the previous formula. 
% 1:28:22
So inverting $f$ and then solidifying $\frac{1}{f}$ is just the same thing as doing this here \note{todo ref}.

% 1:28:56
All we need to check now, if we look at those conditions, most of them we already know. It is a localization, kind of by construction, the localizations commute, because they are both given by $\RHom$'ing out of some object. 
Two functors $\RHom$'ing out of an object commute with each other, just because the tensor product by adjunction, and the tensor product being commutative.

% 1:29:22
What does this \note{todo ref} translate do in terms of these idempotent algebras which determine these localization functors? It translates to a simple condition on these idempotent algebras here \note{todo ref}. 

% 1:29:51
If we take $\Z[[T]]$ and tensor it in solid $\Z$-modules over $\Z[[T]]$ with $\Z((T^{-1}))$, we get zero.
\[
\Z[[T]] \tnsr^\solid_{\Z[T]} \Z[[T^{-1}]] = 0
\]

% 1:30:02
If we have something that dies on $\RHom$ out of this \note{todo ref} and dies on $\RHom$ out of that \note{todo ref}, then by messing around using this \note{todo ref} condition, we conclude that it just has to be zero.

\[
\Z[[T, U]] / 1 - UT
\]

% 1:30:30
You can use the geometric series to see that this is zero.

% 1:30:35
What is the interpretation here? We can think of this $U(\frac{1}{f})$ \note{todo ref} as localizing away from the open unit disc. This $U(\frac{f}{1})$ was localizing to the closed unit disc, or away from the open unit disc centered at infinity.
% 1:31:05
The reason those two cover intuitively, is because if we take the closed unit disc centered at infinity and the closed unit disc centered at zero, then their union is the whole space. 

% 1:31:20
But in terms of the complements, that is saying if we take the open unit disc and the open unit disc at infinity, then they do not intersect. That is exactly the algebraic translation of that fact.

% 1:31:33
Similarly, for the second kind of cover, you need that $\Z[[T]]$ tensor
\[
\Z[[T]] \tnsr_{\Z[T]} \Z[[1-T]] = 0.
\]

But again, this is just
\[
\Z[[T, U]] / 1 - (U+T) = 0
\]

% 1:32:00
This is also zero for the same reason.

% 1:32:05
But again, the interpretation is that the open unit disk centered at 0 intersect the open unit disk centered at 1, they don't intersect. That sounds strange until you remember we are doing non-archimedean geometry, and then it sounds reasonable again.

% 1:32:35
I want to finish with a couple of remarks.

\begin{remark}
So there is the corollary, that if $R$ is any solid ring, $1 \in R^+ \subs $ \note{todo}

% 1:33:10
So now you have to be a little bit careful. I could say that $D(R, R^+)$ localizes on the valuative spectrum $\Spv(R(\ast), R^+)$ of this discrete ring. But be careful. So there is something that is formal which is what I said, that on the global sections, that this thing is just $R$-modules in the category we assigned to this \note{todo} discrete Huber pair.

% 1:3345
But if we then want to get a sheaf, we send a rational open $U$. We have to send that to $R$-modules in $D(\O(u), \O(u))$.
\[
U \mapsto \Mod_R ( D(\O(u), \O(u)) )
\]
I don't, maybe I want to say, so $O$ for the discrete ring, okay?

% todo
So this recovers the topology in the $G$-model, nisr. 

% 1:34:27
So in particular, what is the unit object in this category? We take $R$, and then we invert $G$, and then we derive-solidify with respect to all the $f_I$ over $g$'s.
\[
\text{unit object} = R[\frac{1}{g}]
\]
\note{todo, finish equation}

% 1:35:00
When we do this object for a completely general solid ring, we are necessarily going to end up with some derived phenomena here.

I was hoping to get to it today, but we will probably discuss exactly how that happens later. 

\end{remark}

\begin{remark}

% 1:35:16
I want to also make another remark, which is that this sheaf, $D(R, R^+)$, actually lives over, localizes on this big topological space for the discrete ring, but actually lies over a much smaller closed subset $\Spv(R(\ast), R^+, R^{\circ\circ})$.

% 1:35:50
If we remember the set of topologically nilpotent elements, then here \note{todo} we add the condition that if $f$ here is topologically nilpotent, the valuation has to be strictly less than one. \note{todo, add equation}

% 1:36:05
So for an individual $f$, that is a closed subset, and then it is a big intersection of such things that is a closed subset of this topological space. My claim is just that if we take this sheaf of categories and we restrict it to the open complement, we get zero. So it is really living over this closed subset here\note{todo, add ref}.

% 1:36:35
On the other hand, Huber considers $\Spa (R, R^+) $, which is the continuous valuations less than or equal to one on $R^+$. 
\[
\Spa (R, R^+) = \text{continuous valuations $\leq 1$ on $R^+$}
\]

That is the same thing as a potentially smaller subset satisfying a stronger condition, saying that if we are topologically nilpotent, then for all $\gamma$ in $\Gamma$, there exists an $N \in \N$ such that $v(f^N) \lt \gamma$. So there is some subtlety here.

% continuing the remark 1:38:10

This $D(R,R^+)$ does \emph{not} live over the subset $\Spa(R, R^+) \subse \Spv(R, R^+)$. I claim here \note{todo, add ref}, that the sheaf here becomes zero when you restrict to the complement of this subset. That is not what is happening here, but there does exist a retraction
\[
\Spv(R, R^+} \xrightarrow{r} \Spa(R, R^+}
\]
realizing $\Spa(R, R^+}$ as a \emph{quotient}\footnote{By definition, it was a subspace, but we can actually realize it as a quotient}, and then you do get a sheaf of categories on $\Spa(R, R^+}$. That is the "correct" way to get a sheaf of categories on Huber's topological space. It is the retraction that is kind of the good map, in the sense that this is the quasi-compact map.

\end{remark}

% 1:37:42
The space that Huber localizes over is actually smaller than the space that \emph{we} localize over. But the distinction only occurs for higher ring valuations, but I shouldn't say \emph{only}, because in higher dimensions, higher ring valuations are just everywhere. So there is actually a huge difference.

% 1:38:04
%But Huber shows,

% 1:39:40
So in general, we get more flexibility for localization using this picture than with Huber's picture.

% 1:39:49
The kinds of extra things we get are something that we already discussed, like the so-called functions on the closed unit disc
\[
\Z_p[[x]][\frac{1}{p}].
\]

% 1:39:55
These arise from the structure sheaf in this general setting, but do not arise from the structure sheaf in Huber's setting. We can analyze these things, but I think I have said enough now.

% end of lesson

% \section{Q&A}

% 1:40:10
\gabber So as far as I can remember, in this retraction context, ... the inclusion is not spectral.

\ans Yes.

\gabber The retraction is spectral?

\ans Yes, exactly.

% 1:40:20
\gabber Moreover, is it such that when you have a sheaf on the bigger thing, then the direct image, by the retraction, is the same as the restriction to the subspace. 

\ans For an arbitrary sheaf? No.

\gabber As far as I remember...

% 1:40:40
\ans Let me  make the...

\gabber So it is a nice situation where in particular the higher direct images, so the retraction are zero because of this

\ans Yeah.

\gabber Now you're dealing with sheaves in this infinity setting, so probably the same thing holds, but I'm not...

% 1:41:00
\ans Yeah, so there is the basis of rational opens here \note{todo, add ref}. We can actually parameterize it by similar data, but with an extra condition that these things generate an open ideal. If we pull those rational opens back here \note{todo, add ref}, we get exactly the corresponding rational opens as expected. 
% 1:41:22
But if we take a general rational open here \note{todo, add ref} not satisfying that condition, and then restrict it---no, if it does satisfy that condition and we restrict it, we get the correct thing. 

% 1:41:31
But if it does not satisfy that condition and we restrict it, we get something new, which is not necessarily even quasi-compact, not a rational open. So we have to write it as a union of rational opens. This is like taking the open unit disc and writing it as a union of closed unit discs, which is a typical example of that phenomenon.

% 1:41:56
\ques You said something about getting a structural sheaf last time. Can you comment on this, or will it come later? 

% 1:42:05
\ans I was hoping to get to it again today, but I didn't. So the structural sheaf would just be, we take $R$, which is living globally, and we apply the localization functor to get something living here \note{todo, add ref} instead. That is this object here \note{todo, add ref}, and one can analyze it and so on and so forth. 

% 1:42:26
\ques It is also some center of a certain category. Is there a way to think of it as a center of a category? 

% 1:42:32
\ans I don't know, but these are your symmetric monoidal categories, so it is just the unit.

\gabber the unit of the symmetric monoidal \emph{derived} category in this sense.

\ans Yes.

% 1:42:50
\gabber You claim that when you take $\Mod_R$ of this, this is actually a good thing, which, so it is associated to the---in good cases it is associated to the Huber pair associated to the rational---

\ans Yes.

% 1:43:05
\gabber Except that sometimes you have to do derived. 

\ans Yes. This \note{todo, add ref} will also correspond to an analytic ring, but in the \emph{derived} sense.

% 1:43:20
In the notion of analytic ring that we have discussed so far, we had an ordinary condensed ring in a full subcategory. Here, we need to not just remember that ordinary derived. We need to remember some derived enhancement of it as well. But then it \emph{is} enough to just remember the ordinary abelian category of modules over the ordinary thing.

% 1:43:38
Besides the derived stuff, there is also a quasi-separated issue, where the value of the structure sheaf might be different from Huber's setting, even if it lives in degree zero. 
The quotient might not be by a closed ideal, so it might still differ from Huber's setting. But again, in practical cases, that does not show up. I guess even inverting $g$ \note{todo, add ref} can introduce non-quasi-separated behavior in general. We will discuss all of these this in coming lectures.

% 1:44:05
\gabber So, considering those two spaces in Huber's theory, where we have a retraction, which I think Huber maybe used a slightly different notation, but anyway, this $\Spv$ and $\A^i$. 
% 1:44:15
So we have this subspace living in a slightly bigger thing, and there is a retraction which is spectral. So we have shifts, we can consider shifts on both things. 
% 1:44:25
What I said, I think, is correct, that the restriction to the subspace is like the direct image. Then, we have a sheaf, like in this sheaf of categories and some sense on the full thing, and then we take the direct image---

% 1:44:40
\ans Yes.

\gabber ---to the subspace, which is like restriction. Something I probably, but the question can also be asked about... So we have particular sheaves on the full thing, which are direct images by the inclusion of sheaves on the subspace. 
% 1:44:47
So the question is, like, in this context, so you have your, let us say, you have a rational open in the bigger thing, and you consider its intersection with the smaller subspace.
% 1:45:10 
You said that you can write it as the union of $\C$ \note{not sure}, so you can evaluate your sheaf by inverse limit of those. 

% 1:45:18
\ans Yes.

\gabber Is it equivalent to the value on the original thing in the big $\Spv$? 
So like, whether the sheaf of categories \emph{is} the direct image of its restriction to the subspace?

% 1:45:35
\gabber So you have on $\Spv (R, R^+)$, you have let us say some stack, let us say, of categories, you can take the direct image to, which I think is the same, is a restriction to that?

% 1:45:50
\ans Yes.

\gabber And then, it is a canonical adjunction map for to our lower star to the... let's say is the inclusion...

% 1:46:02
\ans It is not the same because, for example, on global sections, you can see something like ... Well, it is not the same, because for the category, you have a rational subset corresponding to the closed unit disc, giving you this \note{todo, add ref}. And in this category of modules of this guys \note{todo, add ref}, say, the unit is compact. But when you go through that procedure again...

% 1:46:28
\gabber Analytic function is not restricted \note{not sure}. The global analytic function is just... certain... where you don't invert a fixed power of \note{not heard}, you just have convergence on each...

% 1:46:35
\ans Yes.

\gabber So you get another ring of functions, this is something else, this is another theory, so it is not, so the answer is "not".

% 1:46:42
\ans No, there is really more data in the structure sheaf on this guy \note{todo, add ref}.

\gabber What one can do in usual $p$-adic analysis \note{not sure}.

\ans Exactly.

\end{unfinished}